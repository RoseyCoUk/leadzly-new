---
phase: 02-page-restructure-scaffolding
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - page.css
autonomous: true
requirements:
  - DSGN-02

must_haves:
  truths:
    - "Scrolling the page reveals a visible alternating background rhythm: white → light grey → white → dark navy → light grey → green-tinted → white → light grey → dark navy → light grey → white → green-tinted across sections"
    - "The multi-channel section (#channels) displays white text headings and white card text on its dark navy background — no invisible text"
    - "The comparison scaffold (#comparison) displays white text on its dark navy background"
    - "The pain points, industries, and booking scaffold sections have the correct background colours without needing inline styles"
    - "All new section CSS follows the established padding pattern using clamp() and CSS custom property spacing tokens"
  artifacts:
    - path: "page.css"
      provides: "Background colour declarations and full CSS for .pain-points, .channels, .industries, scaffold sections"
      contains: ".pain-points"
    - path: "page.css"
      contains: ".channels"
    - path: "page.css"
      contains: ".industries"
    - path: "page.css"
      contains: ".testimonials"
    - path: "page.css"
      contains: ".comparison"
    - path: "page.css"
      contains: ".faq"
    - path: "page.css"
      contains: ".booking"
    - path: "page.css"
      contains: ".case-studies"
    - path: "page.css"
      contains: ".scaffold-placeholder"
  key_links:
    - from: "page.css .channels"
      to: "var(--color-dark)"
      via: "background property"
      pattern: "background: var\\(--color-dark\\)"
    - from: "page.css .channels .section-title"
      to: "#ffffff"
      via: "explicit colour override for dark section"
      pattern: "color: #ffffff"
    - from: "page.css .comparison"
      to: "var(--color-dark)"
      via: "background property"
      pattern: "background: var\\(--color-dark\\)"
---

<objective>
Add all background colour declarations and full CSS rules for the three new content sections (pain points, multi-channel, industries) plus background-only rules for scaffold sections (case studies, testimonials, comparison, FAQ, booking) to page.css.

Purpose: Give every section its correct background colour per the DSGN-02 alternation pattern and make new content sections visually complete. Dark navy sections must get explicit white text overrides — this plan resolves the critical pitfall where text becomes invisible on dark backgrounds.
Output: page.css extended with complete CSS for all new and scaffold sections. No changes to index.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-page-restructure-scaffolding/02-RESEARCH.md
@.planning/phases/02-page-restructure-scaffolding/02-01-SUMMARY.md

<interfaces>
<!-- Design token values extracted from style.css — use these exact tokens, never hardcode colours -->

CSS Custom Properties (from style.css :root):
  --color-bg:               #ffffff          /* White sections */
  --color-surface:          #f9fafb          /* Light grey sections */
  --color-primary-soft:     #f0fdf4          /* Green-tinted sections */
  --color-dark:             #0f172a          /* Dark navy sections */
  --color-dark-surface:     #1e293b          /* Card bg inside dark sections */
  --color-dark-border:      #334155          /* Card border inside dark sections */
  --color-primary:          #16a34a          /* Green — labels on dark sections */
  --color-primary-highlight: #dcfce7         /* Light green chip background */
  --color-text:             #1a1a2e          /* Body text (dark, DO NOT use on dark bg) */
  --color-text-muted:       #6b7280          /* Muted text (DO NOT use on dark bg) */
  --color-text-faint:       #9ca3af          /* Faint text (for placeholder text) */
  --color-text-inverse:     #ffffff
  --color-border:           #d1d5db
  --radius-lg:              0.75rem
  --radius-full:            9999px
  --shadow-sm:              0 1px 2px oklch(0.2 0.01 250 / 0.06)
  --shadow-md:              0 4px 12px oklch(0.2 0.01 250 / 0.08)
  --space-2: 0.5rem  --space-3: 0.75rem  --space-4: 1rem   --space-5: 1.25rem
  --space-6: 1.5rem  --space-8: 2rem     --space-10: 2.5rem --space-12: 3rem
  --space-16: 4rem   --space-24: 6rem
  --text-xs: clamp(0.75rem, 0.7rem + 0.25vw, 0.875rem)
  --text-sm: clamp(0.875rem, 0.8rem + 0.35vw, 1rem)
  --text-base: clamp(1rem, 0.9rem + 0.5vw, 1.125rem)
  --font-display: 'Plus Jakarta Sans', 'Inter', sans-serif
  --font-body: 'Inter', 'Plus Jakarta Sans', sans-serif
  --transition-interactive: 180ms cubic-bezier(0.16, 1, 0.3, 1)

Existing section padding pattern from page.css (must match exactly):
  padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;

Existing card pattern from page.css (.service-card):
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-8);
  box-shadow: var(--shadow-sm);

Existing grid breakpoints from page.css (use ONLY these):
  Mobile: 1fr (default)
  640px:  repeat(2, 1fr)
  1024px: repeat(3, 1fr)  OR  repeat(4, 1fr) depending on card count

Existing chip pattern from page.css (.result-card__tag):
  display: inline-flex; align-items: center; gap: var(--space-2);
  font-size: var(--text-sm); font-weight: 600; color: var(--color-primary);
  background: var(--color-primary-highlight); padding: var(--space-3) var(--space-5);
  border-radius: var(--radius-full);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add background alternation CSS for all sections</name>
  <files>page.css</files>
  <read_first>
    - page.css (read the full file to find the correct insertion point — append after the last existing section block)
    - style.css (confirm token names before using — especially --color-surface, --color-primary-soft, --color-dark)
    - .planning/phases/02-page-restructure-scaffolding/02-RESEARCH.md (background alternation map table)
  </read_first>
  <action>
Append the following CSS blocks to page.css after the last existing section rule. Do NOT modify any existing rules.

**DSGN-02 Background Alternation Map to implement:**
| Section | Background | Token |
|---------|------------|-------|
| #trust | White | `var(--color-bg)` — already correct (no bg set), no action needed |
| #pain-points | Light grey | `var(--color-surface)` |
| #services | White | `var(--color-bg)` — already correct, no action needed |
| #channels | Dark navy | `var(--color-dark)` + white text overrides |
| #how-it-works | Light grey | already `var(--color-surface)` — correct, no action needed |
| #industries | Green-tinted | `var(--color-primary-soft)` |
| #case-studies | White | `var(--color-bg)` — already correct (was .results), verify |
| #testimonials | Light grey | `var(--color-surface)` |
| #comparison | Dark navy | `var(--color-dark)` + white text overrides |
| #pricing | Light grey | already `var(--color-surface)` — correct, no action needed |
| #faq | White | `var(--color-bg)` |
| #booking | Green-tinted | `var(--color-primary-soft)` |
| #cta | Green gradient | already done (.cta-banner) — no action needed |

Append this CSS block to page.css:

```css
/* ============ PHASE 2: SECTION BACKGROUNDS & ALTERNATION (DSGN-02) ============ */

/* Pain Points — light grey */
.pain-points {
  background: var(--color-surface);
}

/* Case Studies — white (renamed from .results) */
.case-studies {
  background: var(--color-bg);
}

/* Testimonials scaffold — light grey */
.testimonials {
  background: var(--color-surface);
}

/* Comparison scaffold — dark navy */
.comparison {
  background: var(--color-dark);
}
/* Text colour overrides for dark comparison section */
.comparison .section-label {
  color: var(--color-primary);
}
.comparison .section-title {
  color: #ffffff;
}
.comparison .section-subtitle {
  color: rgba(255, 255, 255, 0.7);
}
.comparison .scaffold-placeholder {
  color: rgba(255, 255, 255, 0.5);
}

/* FAQ — white */
.faq {
  background: var(--color-bg);
}

/* Booking scaffold — green-tinted */
.booking {
  background: var(--color-primary-soft);
}

/* Scaffold placeholder text (Phase 2 only — phases 3–5 replace these) */
.scaffold-placeholder {
  text-align: center;
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: var(--color-text-faint);
  padding: var(--space-8) 0;
  font-style: italic;
}
```
  </action>
  <verify>
    - `grep -n "\.pain-points" page.css` returns a result with `background: var(--color-surface)`
    - `grep -n "\.comparison" page.css` returns a result with `background: var(--color-dark)`
    - `grep -n "\.comparison .section-title" page.css` returns a result with `color: #ffffff`
    - `grep -n "\.booking" page.css` returns a result with `background: var(--color-primary-soft)`
    - Open index.html in browser: the comparison scaffold section has a dark navy background with visible white heading text (not invisible dark text on dark background)
  </verify>
  <acceptance_criteria>
    - `grep "background: var(--color-surface)" page.css` returns at least 2 results (pain-points + testimonials)
    - `grep "background: var(--color-dark)" page.css` returns at least 2 results (channels + comparison)
    - `grep "background: var(--color-primary-soft)" page.css` returns at least 2 results (industries + booking)
    - `grep "color: #ffffff" page.css` returns at least 2 results (dark section text overrides)
    - Visual check: Scrolling the full page in browser shows distinctly different background colours alternating between sections — no two adjacent sections have the same background colour
    - Visual check: Text inside the comparison section (#comparison) is legible white on dark navy — headline and subtitle are not invisible
    - The `.scaffold-placeholder` rule exists in page.css with `color: var(--color-text-faint)` and `font-style: italic`
  </acceptance_criteria>
  <done>All DSGN-02 background colours are set. Scaffold sections have correct backgrounds. Dark sections have explicit white text overrides. Alternating background rhythm is visually confirmed in browser.</done>
</task>

<task type="auto">
  <name>Task 2: Add full CSS for pain points, multi-channel, and industries sections</name>
  <files>page.css</files>
  <read_first>
    - page.css (read again after Task 1 to see current state including the Task 1 additions)
    - .planning/phases/02-page-restructure-scaffolding/02-RESEARCH.md (card grid patterns, dark navy pattern, chip pattern)
  </read_first>
  <action>
Append the following CSS blocks to page.css immediately after the Task 1 additions. These are the full layout and card styles for the three new content sections.

```css
/* ============ PHASE 2: NEW SECTION STYLES ============ */

/* ---- PAIN POINTS ---- */
.pain-points {
  padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;
}

.pain-points__grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--space-6);
}
@media (min-width: 640px) {
  .pain-points__grid { grid-template-columns: repeat(2, 1fr); }
}
@media (min-width: 1024px) {
  .pain-points__grid { grid-template-columns: repeat(3, 1fr); }
}

.pain-card {
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-8);
  box-shadow: var(--shadow-sm);
}

.pain-card__icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 48px;
  height: 48px;
  border-radius: var(--radius-lg);
  background: var(--color-primary-highlight);
  color: var(--color-primary);
  margin-bottom: var(--space-4);
  flex-shrink: 0;
}

.pain-card__title {
  font-family: var(--font-display);
  font-size: var(--text-base);
  font-weight: 700;
  color: var(--color-text);
  margin-bottom: var(--space-3);
  line-height: 1.4;
}

.pain-card__desc {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: var(--color-text-muted);
  line-height: 1.6;
  margin: 0;
}

/* ---- MULTI-CHANNEL BREAKDOWN ---- */
.channels {
  padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;
  background: var(--color-dark);
}

/* Dark section text overrides */
.channels .section-label {
  color: var(--color-primary);
}
.channels .section-title {
  color: #ffffff;
}
.channels .section-subtitle {
  color: rgba(255, 255, 255, 0.7);
}

.channels__grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--space-6);
}
@media (min-width: 640px) {
  .channels__grid { grid-template-columns: repeat(2, 1fr); }
}
@media (min-width: 1024px) {
  .channels__grid { grid-template-columns: repeat(4, 1fr); }
}

.channel-card {
  background: var(--color-dark-surface);
  border: 1px solid var(--color-dark-border);
  border-radius: var(--radius-lg);
  padding: var(--space-8);
}

.channel-card__icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 48px;
  height: 48px;
  border-radius: var(--radius-lg);
  background: rgba(22, 163, 74, 0.15);
  color: var(--color-primary);
  margin-bottom: var(--space-4);
  flex-shrink: 0;
}

.channel-card__title {
  font-family: var(--font-display);
  font-size: var(--text-base);
  font-weight: 700;
  color: #ffffff;
  margin-bottom: var(--space-3);
}

.channel-card__desc {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  color: rgba(255, 255, 255, 0.7);
  line-height: 1.6;
  margin: 0;
}

/* ---- INDUSTRIES SERVED ---- */
.industries {
  padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;
  background: var(--color-primary-soft);
}

.industries__chips {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-3);
  justify-content: center;
}

.industry-chip {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2);
  font-family: var(--font-body);
  font-size: var(--text-sm);
  font-weight: 600;
  color: var(--color-primary);
  background: #ffffff;
  padding: var(--space-3) var(--space-5);
  border-radius: var(--radius-full);
  border: 1px solid rgba(22, 163, 74, 0.2);
  box-shadow: var(--shadow-sm);
  white-space: nowrap;
}

.industry-chip svg {
  flex-shrink: 0;
}

/* ---- SCAFFOLD SECTIONS (padding only — backgrounds set in DSGN-02 block above) ---- */
.testimonials {
  padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;
}

.comparison {
  padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;
}

.faq {
  padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;
}

.booking {
  padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;
}

/* Case Studies inherits from .results patterns — add class alias */
.case-studies {
  padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;
  background: var(--color-bg);
}
```

**PITFALL CHECK before finishing:** After appending, verify that `.channels` and `.pain-points` and `.industries` do NOT have their `background` declared twice (once in the Task 1 block above and again in this block). If they do, remove the duplicate from the Task 1 block (the full rule in this task is authoritative). Check by reading page.css and searching for duplicate `.channels {` declarations.
  </action>
  <verify>
    - `grep -c "\.channels {" page.css` — should return exactly 1 (no duplicates)
    - `grep -c "\.pain-points {" page.css` — should return exactly 1
    - `grep -c "\.industries {" page.css` — should return exactly 1
    - Open index.html in browser:
      - Pain points section: 6 cards in a 2-col (tablet) / 3-col (desktop) grid on light grey background, each card has green icon box
      - Multi-channel section: 4 cards in a 4-col desktop grid on dark navy background, white text visible, green icon boxes
      - Industries section: 12 chips in a flex-wrap layout on green-tinted background, white chip backgrounds with green text and border
  </verify>
  <acceptance_criteria>
    - `grep "\.pain-card__icon" page.css` returns a result with `background: var(--color-primary-highlight)`
    - `grep "\.channel-card__title" page.css` returns a result with `color: #ffffff`
    - `grep "\.channel-card__desc" page.css` returns a result with `color: rgba(255, 255, 255, 0.7)`
    - `grep "\.industry-chip" page.css` returns a result with `border-radius: var(--radius-full)`
    - `grep "\.industries__chips" page.css` returns a result with `flex-wrap: wrap`
    - `grep "grid-template-columns: repeat(4, 1fr)" page.css` returns a result (channels 4-col grid)
    - `grep "grid-template-columns: repeat(3, 1fr)" page.css` returns a result (pain-points 3-col grid)
    - Visual check: Pain points section shows 6 distinct white cards with green icon circles and readable dark text on light grey background
    - Visual check: Multi-channel section shows 4 dark navy cards (#1e293b background) with green icon circles and white card titles and body text — NO invisible text
    - Visual check: Industries section shows 12 white pill-shaped chips with green text on a green-tinted (#f0fdf4) background
    - Visual check: No two adjacent sections share the same background colour when scrolling the full page
    - No `px` or hardcoded colour hex values used in new CSS rules (all use `var(--*)` tokens or the explicit white `#ffffff` override on dark sections as documented in research)
  </acceptance_criteria>
  <done>page.css contains complete styles for .pain-points, .channels, and .industries including grids, cards, icons, and text. Scaffold sections have correct padding. Dark sections have white text overrides. All spacing and colours use design system tokens. No two adjacent sections share the same background colour on scroll.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 02 complete: DSGN-02 background alternation and full CSS for all new sections.
    - page.css extended with background rules for all 14 sections
    - Full card CSS for pain points (6 cards), multi-channel (4 cards), industries (12 chips)
    - Scaffold section padding for testimonials, comparison, FAQ, booking
    - Dark navy sections (multi-channel, comparison) have white text overrides
  </what-built>
  <how-to-verify>
    Open index.html in a browser (no server needed — open directly as a file). Scroll the full page slowly from top to bottom and confirm:

    1. BACKGROUND ALTERNATION — Adjacent sections have visibly different backgrounds:
       - Hero (dark navy) → Trust bar (white) → Pain Points (light grey) → Services (white) → Multi-Channel (dark navy) → Process (light grey) → Industries (green-tinted) → Case Studies (white) → Testimonials (light grey) → Comparison (dark navy) → Pricing (light grey) → About (white) → FAQ (white)* → Booking (green-tinted) → Footer CTA (green gradient) → Footer (dark navy)
       *(FAQ and About are both white — this is acceptable as About is a temporary placeholder; Phase 3 will move About)

    2. PAIN POINTS SECTION — Light grey background. 6 cards in a grid. Each card has a green icon circle. Card titles are dark, readable. No invisible text.

    3. MULTI-CHANNEL SECTION — Dark navy background (#0f172a). 4 cards with slightly lighter backgrounds. Card titles are WHITE and clearly readable. Card body text is white/translucent and readable. Green icon boxes visible. (If titles or body text are invisible/dark-on-dark, that is a fail.)

    4. INDUSTRIES SECTION — Green-tinted background (#f0fdf4). 12 white pill chips in a flex-wrap layout. Each chip has a green icon and green text label.

    5. COMPARISON SCAFFOLD — Dark navy background. Heading text is WHITE and readable. Placeholder italic text is visible (light grey/faint colour).

    6. BOOKING SCAFFOLD — Green-tinted background. Heading text is readable.

    If any section has invisible text (dark text on dark background), report it and it will be fixed before proceeding.
  </how-to-verify>
  <resume-signal>Type "approved" if all backgrounds and text colours look correct, or describe any sections where text is invisible or backgrounds are wrong.</resume-signal>
</task>

</tasks>

<verification>
Full phase 2 verification checklist (run after both plans complete):

1. Section order: `document.querySelectorAll('main > section').map(s => s.id)` in DevTools should return:
   ["hero", "trust", "pain-points", "services", "channels", "how-it-works", "industries", "case-studies", "testimonials", "comparison", "pricing", "about", "faq", "booking", "cta"]

2. Card counts:
   - `document.querySelectorAll('#pain-points .pain-card').length` → 6
   - `document.querySelectorAll('#channels .channel-card').length` → 4
   - `document.querySelectorAll('#industries .industry-chip').length` → 12

3. Background colours (computed style check in DevTools):
   - `getComputedStyle(document.querySelector('#channels')).backgroundColor` → `rgb(15, 23, 42)` (dark navy)
   - `getComputedStyle(document.querySelector('#industries')).backgroundColor` → `rgb(240, 253, 244)` (green-tinted)
   - `getComputedStyle(document.querySelector('#pain-points')).backgroundColor` → `rgb(249, 250, 251)` (light grey)

4. No dead anchors: `grep 'href="#results"' index.html` → no results
</verification>

<success_criteria>
- DSGN-02: Every section has a distinct background colour matching the alternation map (white, light grey, dark navy, green-tinted cycling through the page)
- Dark sections (#channels, #comparison) have legible white text — no invisible body text
- All new section CSS uses var(--*) design tokens for spacing, with explicit #ffffff only for dark section text overrides
- Pain points, multi-channel, and industries sections are visually complete (grid layout, card styles, icon circles)
- Scaffold sections (testimonials, comparison, FAQ, booking) have correct backgrounds and readable placeholder text
- No duplicate CSS class declarations in page.css
</success_criteria>

<output>
After completion, create `.planning/phases/02-page-restructure-scaffolding/02-02-SUMMARY.md`
</output>

---
phase: 03-trust-social-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bluepillar-logo.png
  - boltloop_logo.png
  - elevateo_logo.png
  - flooda_logo.png
  - roseyCo_logo.png
  - index.html
  - page.css
autonomous: true
requirements:
  - TRST-05

must_haves:
  truths:
    - "A slim trust bar band is visible directly below the hero section and above the pain points section"
    - "Five partner logos render greyscale at ~0.6 opacity in the trust bar"
    - "A '100+ meetings/month' stat badge and a '30-Day Guarantee' badge appear in the trust bar"
    - "Hovering a logo reveals its colour (greyscale filter removed)"
    - "Trust bar collapses cleanly on mobile (divider hidden, logos and badges wrap)"
  artifacts:
    - path: "bluepillar-logo.png"
      provides: "Bluepillar (E-Commerce) logo asset"
    - path: "boltloop_logo.png"
      provides: "Boltloop (AI & Automation) logo asset"
    - path: "elevateo_logo.png"
      provides: "Elevateo (parent company) logo asset"
    - path: "flooda_logo.png"
      provides: "Flooda (SaaS & Software) logo asset"
    - path: "roseyCo_logo.png"
      provides: "Rosey Co (Digital Marketing) logo asset"
    - path: "index.html"
      provides: "Trust bar section HTML between hero and pain points"
      contains: "class=\"trust-bar\""
    - path: "page.css"
      provides: "Trust bar CSS rules"
      contains: ".trust-bar {"
  key_links:
    - from: "index.html .trust-bar section"
      to: "CSS .trust-bar rules in page.css"
      via: "class attribute on section element"
      pattern: "class=\"trust-bar\""
    - from: ".trust-bar__logos img elements"
      to: "logo PNG files in repo root"
      via: "src=\"./filename.png\""
      pattern: "src=\"\\./.*\\.png\""
---

<objective>
Copy 5 partner logo PNG files into the repo root, then insert a trust bar section immediately after the hero closing tag with greyscale logos, a stat badge, and a guarantee badge — styled as a slim horizontal band.

Purpose: TRST-05 — a trust bar directly below the hero is the first credibility signal a prospect sees. The greyscale logo strip signals real clients; the stat and guarantee badges anchor confidence before the reader hits the pain points section.

Output: 5 PNG assets in repo root + trust bar section in index.html + `.trust-bar` CSS block in page.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-trust-social-proof/03-CONTEXT.md
@.planning/phases/03-trust-social-proof/03-UI-SPEC.md
@.planning/phases/03-trust-social-proof/03-RESEARCH.md

<interfaces>
<!-- Key patterns from existing codebase — use these directly. -->

Section insertion point (index.html lines 214–217):
```html
  </section>           <!-- hero closes at line 214 -->

  <!-- ============ PAIN POINTS ============ -->
  <section class="pain-points" id="pain-points">
```
Trust bar HTML inserts between these two lines.

CSS append point: page.css ends with `.case-studies__cta { ... }` at line 1181.
Append all new `.trust-bar` CSS after the final closing brace.

Confirmed CSS token names (from style.css :root):
- `--section-tint`   (#f8fafb)  — trust bar background (D-11)
- `--color-divider`             — 1px vertical divider between logos and badges
- `--color-primary`  (#16a34a)  — badge text colour
- `--color-border`              — subtle border on bottom of trust bar
- `--space-2`, `--space-4`, `--space-6`, `--space-8` — spacing tokens
- `--text-sm`                   — badge font size
- `--font-body`                 — Inter, used in badges
- `--transition-interactive`    — logo hover transition (180ms cubic-bezier)

Logo alt text pattern: `"{Company} — partner"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy logo assets into repo root</name>
  <files>bluepillar-logo.png, boltloop_logo.png, elevateo_logo.png, flooda_logo.png, roseyCo_logo.png</files>
  <read_first>
    - Check repo root first: run `ls *.png` to confirm which files (if any) already exist before copying
    - Source directory: `C:\Work File\Assets\Logos\` — read the directory listing to confirm all 5 filenames exactly
  </read_first>
  <action>
Copy the following 5 files from `C:\Work File\Assets\Logos\` into the repo root (same directory as index.html). Do NOT rename them — filenames must match exactly as specified in D-09:

1. `bluepillar-logo.png`
2. `boltloop_logo.png`
3. `elevateo_logo.png`
4. `flooda_logo.png`
5. `roseyCo_logo.png`

Use bash cp commands with exact source/destination paths. Example:
```
cp '/c/Work File/Assets/Logos/bluepillar-logo.png' '/c/Work File/Elevateo Work/Leadzly - Company/bluepillar-logo.png'
```
Repeat for each of the 5 files.
  </action>
  <verify>
    <automated>ls '/c/Work File/Elevateo Work/Leadzly - Company/bluepillar-logo.png' '/c/Work File/Elevateo Work/Leadzly - Company/boltloop_logo.png' '/c/Work File/Elevateo Work/Leadzly - Company/elevateo_logo.png' '/c/Work File/Elevateo Work/Leadzly - Company/flooda_logo.png' '/c/Work File/Elevateo Work/Leadzly - Company/roseyCo_logo.png'</automated>
  </verify>
  <acceptance_criteria>
    - `ls bluepillar-logo.png` exits 0 (file exists in repo root)
    - `ls boltloop_logo.png` exits 0
    - `ls elevateo_logo.png` exits 0
    - `ls flooda_logo.png` exits 0
    - `ls roseyCo_logo.png` exits 0
    - All 5 files have non-zero file size (not empty copies)
  </acceptance_criteria>
  <done>All 5 logo PNG files exist in the repo root directory alongside index.html.</done>
</task>

<task type="auto">
  <name>Task 2: Insert trust bar HTML and CSS</name>
  <files>index.html, page.css</files>
  <read_first>
    - `index.html` — read lines 210–220 to confirm the exact hero close tag and pain-points open tag so the insertion point is correct
    - `page.css` — read the last 20 lines to see the current file end and confirm where to append the new CSS block
    - `style.css` — if in doubt about any token name, grep for `--section-tint\|--color-divider\|--color-primary\b\|--transition-interactive` to confirm they exist
  </read_first>
  <action>
**Step 1 — Insert trust bar HTML into index.html**

Find the line `  </section>` that closes the hero (line ~214, immediately followed by the `<!-- PAIN POINTS -->` comment). Insert the following HTML block between the hero close tag and the pain-points section:

```html
  <!-- ============ TRUST BAR ============ -->
  <section class="trust-bar" aria-label="Social proof">
    <div class="trust-bar__inner container">
      <div class="trust-bar__logos">
        <img src="./elevateo_logo.png" alt="Elevateo — partner" loading="lazy">
        <img src="./roseyCo_logo.png" alt="Rosey Co — partner" loading="lazy">
        <img src="./boltloop_logo.png" alt="Boltloop — partner" loading="lazy">
        <img src="./flooda_logo.png" alt="Flooda — partner" loading="lazy">
        <img src="./bluepillar-logo.png" alt="Bluepillar — partner" loading="lazy">
      </div>
      <div class="trust-bar__divider" aria-hidden="true"></div>
      <div class="trust-bar__badges">
        <span class="trust-bar__badge trust-bar__badge--stat">100+ meetings/month</span>
        <span class="trust-bar__badge trust-bar__badge--guarantee">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="20 6 9 17 4 12"/></svg>
          30-Day Guarantee
        </span>
      </div>
    </div>
  </section>
```

**Step 2 — Append trust bar CSS to page.css**

Append the following CSS block after the last line of page.css (after `.case-studies__cta { ... }`):

```css

/* ============ TRUST BAR ============ */
.trust-bar {
  background: var(--section-tint);
  padding: var(--space-4) 0;
  border-bottom: 1px solid var(--color-border);
}
.trust-bar__inner {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-8);
  flex-wrap: wrap;
}
.trust-bar__logos {
  display: flex;
  align-items: center;
  gap: var(--space-6);
  flex-wrap: wrap;
  justify-content: center;
}
.trust-bar__logos img {
  max-height: 40px;
  width: auto;
  filter: grayscale(100%);
  opacity: 0.6;
  transition: filter var(--transition-interactive), opacity var(--transition-interactive);
}
.trust-bar__logos img:hover {
  filter: grayscale(0%);
  opacity: 1;
}
.trust-bar__divider {
  width: 1px;
  height: 32px;
  background: var(--color-divider);
}
@media (max-width: 639px) {
  .trust-bar__divider { display: none; }
}
.trust-bar__badges {
  display: flex;
  align-items: center;
  gap: var(--space-4);
  flex-wrap: wrap;
  justify-content: center;
}
.trust-bar__badge {
  display: inline-flex;
  align-items: center;
  gap: var(--space-2);
  font-family: var(--font-body);
  font-size: var(--text-sm);
  font-weight: 700;
  color: var(--color-primary);
}
```

**Pitfall guard:** Confirm the trust bar `<section>` is inserted OUTSIDE and AFTER the `</section>` that closes `<section class="hero" id="hero">` — not inside it. The hero section has a dark background; the trust bar must appear as a separate light band. If the trust bar appears on a dark background in the browser, the insertion point is wrong.
  </action>
  <verify>
    <automated>grep -n "class=\"trust-bar\"" '/c/Work File/Elevateo Work/Leadzly - Company/index.html' && grep -n "\.trust-bar {" '/c/Work File/Elevateo Work/Leadzly - Company/page.css'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class=\"trust-bar\"" index.html` returns 1 (exactly one trust-bar section)
    - `grep -n "class=\"trust-bar\"" index.html` shows a line number GREATER than the hero section line (148) and LESS than the pain-points section line (currently ~217, will shift slightly after insertion)
    - `grep "src=\"./elevateo_logo.png\"" index.html` returns a match
    - `grep "src=\"./bluepillar-logo.png\"" index.html` returns a match
    - `grep "100+ meetings/month" index.html` returns a match
    - `grep "30-Day Guarantee" index.html` returns a match (inside trust-bar__badges)
    - `grep "\.trust-bar {" page.css` returns a match
    - `grep "grayscale(100%)" page.css` returns a match
    - `grep "var(--section-tint)" page.css` returns a match inside the `.trust-bar` block (not `--section-grey` which does not exist)
    - `grep "var(--transition-interactive)" page.css` returns a match inside `.trust-bar__logos img`
  </acceptance_criteria>
  <done>Trust bar section exists in index.html between the hero and pain-points sections. All 5 logo images are referenced with correct filenames. Both stat and guarantee badges are present. CSS is complete in page.css with no hardcoded hex values.</done>
</task>

</tasks>

<verification>
After both tasks complete, open index.html in a browser and verify:
1. A slim light-grey (#f8fafb) band is visible immediately below the hero dark section
2. Five logos render in greyscale at reduced opacity
3. Hovering a logo reveals its colour
4. "100+ meetings/month" and "30-Day Guarantee" text appear to the right of the logos (separated by a divider on desktop)
5. On a narrow viewport (resize browser to <640px), the divider hides and logos/badges wrap to multiple rows cleanly
</verification>

<success_criteria>
- Five logo files exist in repo root (ls exits 0 for all five)
- `grep -c "class=\"trust-bar\"" index.html` returns 1
- Trust bar appears between hero close tag and pain-points open tag in the HTML source
- `.trust-bar` CSS block exists in page.css with `var(--section-tint)` background
- No hardcoded hex colours in the new CSS block
</success_criteria>

<output>
After completion, create `.planning/phases/03-trust-social-proof/03-01-SUMMARY.md` following the summary template.
</output>

---
phase: 4
slug: conversion-sections
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-27
---

# Phase 4 — UI Design Contract: Conversion Sections

> Visual and interaction contract for Phase 4 frontend implementation. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — vanilla HTML/CSS/JS only |
| Preset | not applicable |
| Component library | none |
| Icon library | inline SVG only (no external library) |
| Font | Inter (body) + Plus Jakarta Sans (display headings) — both pre-loaded via Google Fonts |

**shadcn gate result:** Not applicable. Stack is HTML/CSS/vanilla JS — no React, no Vite, no Next.js. shadcn initialization skipped.

---

## Spacing Scale

All spacing uses CSS custom properties declared in `style.css :root`. Executor MUST use tokens, not hardcoded values.

| Token | Value | Usage in this phase |
|-------|-------|---------------------|
| `--space-2` | 8px | Table cell icon gaps; FAQ toggle icon gap |
| `--space-3` | 12px | Section-label margin-bottom (existing pattern); table cell padding vertical; `.roi__output-label` margin-bottom |
| `--space-4` | 16px | FAQ item padding vertical; ROI input label spacing; table cell padding horizontal |
| `--space-6` | 24px | FAQ accordion item gap (between questions); ROI output block padding; card-like inner padding |
| `--space-8` | 32px | ROI calculator container padding; comparison table outer padding |
| `--space-10` | 40px | Section header margin-bottom (min via clamp) |
| `--space-12` | 48px | Section padding minimum (via clamp) |
| `--space-16` | 64px | Section header margin-bottom (max via clamp) |
| `--space-24` | 96px | Section padding maximum on large viewport (via clamp) |

**Existing design system exceptions (non-standard scale values):**

The following tokens fall outside the strict 8-point standard set (4, 8, 16, 24, 32, 48, 64px) but are **inherited from the project's `style.css :root`** and are in active use across all existing page sections. They are part of the base design system for this project. Replacing them would require modifying `style.css` and would break existing sections that are already implemented and tested. They are retained as-is:

- `--space-3` = 12px — **Design system exception.** Inherited from `style.css :root`. Used by `.section-label` margin-bottom across all existing sections and by table cell padding vertical. Changing this token would break multiple existing components.
- `--space-10` = 40px — **Design system exception.** Inherited from `style.css :root`. Used as the `clamp()` minimum for `.section-header` margin-bottom across all existing sections. Changing this token would break the section header rhythm project-wide.
- `--space-24` = 96px — **Design system exception.** Inherited from `style.css :root`. Used as the `clamp()` maximum for section vertical padding across all existing sections (`padding: clamp(var(--space-12), 8vw, var(--space-24)) 0`). Changing this token would break the section padding contract for all existing and new sections.

**Pattern for section padding (MUST match all existing sections):**
`padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;`
All three new sections (comparison, FAQ, ROI calculator) MUST use this exact clamp pattern.

**Comparison table column width exception:** The Leadzly column uses `min-width: 160px` on desktop so the green highlight has enough breathing room at any viewport. This is a layout constraint, not a spacing token exception.

**FAQ accordion toggle target:** The `<button>` for each accordion item must be at least 44px tall to meet touch target requirements. Achieved via `padding: var(--space-4) 0` (≈16px top + 16px bottom + ~14px text line = ~46px). No additional spacing token needed.

---

## Typography

All font sizes use fluid clamp tokens from `style.css`. Executor MUST use tokens, not hardcoded px. Maximum 4 size tokens and 2 weight values are permitted in this phase.

| Role | Token | Min/Max (approx) | Weight | Line Height | Usage in this phase |
|------|-------|-----------------|--------|-------------|---------------------|
| Eyebrow / section label | `--text-xs` | 12px / 14px | 700 | n/a (single line) | `.section-label` on all three new sections + hero eyebrow pattern (DSGN-01); table header labels; ROI input labels |
| Body / supporting text | `--text-sm` | 14px / 16px | 400 | 1.6 | Table cell content text; FAQ answer body; ROI helper text; comparison row labels; ROI input text |
| Card heading / FAQ question | `--text-lg` | 18px / 24px | 700 | 1.3 | FAQ question text (closed state); comparison table section heading (if used); ROI output values |
| Section title | `--text-xl` | 24px / 36px | 700 | 1.15 | `.section-title` on all three section headers |

**Hero h1 fix (DSGN-01 — existing rule update):**
The `hero__headline` rule already uses `font-size: clamp(2.4rem, 3.5vw + 0.5rem, 4.25rem)` — the minimum value 2.4rem is below the 3.5rem requirement. Change the clamp minimum to `3.5rem`:
`font-size: clamp(3.5rem, 3.5vw + 0.5rem, 4.75rem);`
Weight is already `800` (satisfies DSGN-01 "bold at ≥3.5rem"). No other heading changes.

**Eyebrow label (DSGN-01 — audit of existing sections):**
`.section-label` already exists with `--text-xs, weight 600, uppercase, letter-spacing 0.08em, color-primary`. Change weight to 700 to match Phase 3 contract (minor consistency fix). Audit all existing section HTML: every `<div class="section-header">` MUST contain `<span class="section-label">`. Any section header missing the eyebrow span gets one added. See Copywriting Contract for all eyebrow strings.

**Font families:**
- Display headings (`.section-title`, FAQ question, ROI output value): `var(--font-display)` → Plus Jakarta Sans
- Body, labels, table cell text, input labels: `var(--font-body)` → Inter

**Weights used in this phase: 400 (regular) and 700 (bold). No other weights.**

---

## Color

All colors use CSS custom properties from `style.css :root`. Executor MUST use tokens.

| Role | Token | Hex | Usage in this phase |
|------|-------|-----|---------------------|
| Dominant (60%) — light surfaces | `--section-white` | #ffffff | FAQ section background; ROI calculator section background |
| Secondary (30%) — dark navy | `--color-dark` | #0f172a | Comparison table section background (SECT-01 explicit requirement) |
| Accent (10%) — brand green | `--color-primary` | #16a34a | Leadzly column highlight background tint; checkmark icons in Leadzly column; `.section-label` text; FAQ open-state indicator; ROI output accent; hero eyebrow text |
| Accent soft | `--color-primary-soft` | #f0fdf4 | Leadzly column background tint (column highlight fill) |
| Accent highlight | `--color-primary-highlight` | #dcfce7 | Leadzly column header background; ROI result block background |
| Dark border | `--color-dark-border` | #334155 | Comparison table row borders on dark background; table outer border |
| Dark surface | `--color-dark-surface` | #1e293b | Comparison table row alternation on dark background |
| Text inverse | `--color-text-inverse` | #ffffff | All text inside comparison table section (dark background) |
| Text muted inverse | `rgba(255,255,255,0.65)` | — | Supporting text inside comparison table (non-Leadzly cell content) |
| Text primary | `--color-text` | #111827 | FAQ question text, ROI labels, section titles |
| Text muted | `--color-text-muted` | #6b7280 | FAQ answer body text, ROI helper text |
| Semantic destructive | `--color-error` | #dc2626 | Not used in Phase 4 (no destructive actions) |

**Accent reserved for (explicit list — this phase):**
- Leadzly column background fill (`--color-primary-soft`) and column header fill (`--color-primary-highlight`) in comparison table
- Checkmark icons (`✓`) rendered in the Leadzly column of comparison table
- `.section-label` eyebrow text on all three new section headers and all existing section headers being audited
- Open-state FAQ indicator (border-left or bottom border on the active question item)
- ROI output block background (`--color-primary-highlight`) and value text (`--color-primary`)
- Focus ring — already defined globally in `base.css` — do not override

Accent is NOT used for: general body text inside dark table section, table row borders, button fills other than `.btn--primary`, background of the full FAQ or ROI sections.

**Section background alternation (continuation of DSGN-02 pattern):**
- Comparison table: `var(--color-dark)` (dark navy — already set in scaffold CSS)
- FAQ: `var(--section-white)` — #ffffff (already set in scaffold CSS)
- ROI Calculator: The scaffold currently has no ROI section. Use `var(--section-tint)` (#f8fafb) to maintain the alternating rhythm (dark navy → white → light tint).

---

## Component Inventory

New CSS classes to be authored in `page.css` for this phase. All shared classes (`.section-header`, `.section-label`, `.section-title`, `.section-subtitle`, `.container`, `.btn`, `.btn--primary`, `.fade-in`) are reused without modification.

---

### Comparison Table — `.comparison` + `.comparison__table`

The `.comparison` section and background are already scaffolded in `page.css`. Replace the `<p class="scaffold-placeholder">` with the table structure.

**HTML structure:**

```
.comparison
  .container
    .section-header
      .section-label       — "Why Leadzly"
      .section-title       — "How We Stack Up"
      .section-subtitle    — "See how Leadzly compares..."
    .comparison__table-wrap  — overflow-x: auto (mobile scroll)
      table.comparison__table
        thead
          tr
            th (empty — row label column)
            th.comparison__th--leadzly  — "Leadzly" (green highlighted)
            th                           — "In-House"
            th                           — "Other Agencies"
        tbody
          tr.comparison__row (×8)
            td.comparison__label        — row criterion text
            td.comparison__cell--leadzly — Leadzly value (green ✓ icon + text)
            td                           — In-House value
            td                           — Other Agencies value
```

**Visual rules:**

| Element | Spec |
|---------|------|
| Section background | `var(--color-dark)` — #0f172a |
| Section label text | `var(--color-primary)` — green on dark |
| Section title + subtitle text | `var(--color-text-inverse)` — #ffffff |
| Table outer border | `1px solid var(--color-dark-border)` |
| Table border-radius | `var(--radius-xl)` on the wrapping div |
| Thead background | `var(--color-dark-surface)` — #1e293b |
| Tbody row alternation | odd rows: `var(--color-dark)`, even rows: `var(--color-dark-surface)` |
| Row border-bottom | `1px solid var(--color-dark-border)` |
| Row label cell (col 1) | `--text-sm`, weight 400, color `rgba(255,255,255,0.65)`, padding `var(--space-4)` |
| Leadzly column header (`th.comparison__th--leadzly`) | background `var(--color-primary-highlight)`, color `var(--color-primary)`, font weight 700, `--text-sm` |
| Leadzly column cells (`td.comparison__cell--leadzly`) | background `var(--color-primary-soft)`, color `var(--color-primary)`, font weight 700, `--text-sm` |
| Non-Leadzly header cells | background `var(--color-dark-surface)`, color `rgba(255,255,255,0.85)`, `--text-sm` weight 700 |
| Non-Leadzly body cells | color `rgba(255,255,255,0.65)`, `--text-sm` weight 400 |
| Leadzly ✓ icon | inline SVG checkmark (16×16), stroke `var(--color-primary)`, `stroke-width: 2.5` |
| Non-Leadzly ✗ icon | inline SVG ✗ (16×16), stroke `rgba(255,255,255,0.4)`, `stroke-width: 2` |
| Cell padding | `var(--space-3) var(--space-4)` |

**Mobile:** Wrap table in `.comparison__table-wrap` with `overflow-x: auto; -webkit-overflow-scrolling: touch`. Table min-width: 560px so columns don't collapse into unreadable widths. On mobile this scrolls horizontally (satisfies DSGN-04).

**8 comparison rows — content specification:**

| Row | Leadzly | In-House | Other Agencies |
|-----|---------|----------|----------------|
| Setup time | Ready in 30 days | 3–6 months to hire & ramp | Varies (often slow) |
| Monthly cost | From £1,500/mo | £40,000+/yr salary + tools | Often opaque pricing |
| Meetings guaranteed | ✓ Yes — guaranteed | ✗ No guarantee | ✗ No guarantee |
| UK-based team | ✓ Northern Ireland team | ✓ If you hire locally | ✗ Typically offshore |
| Multi-channel outreach | ✓ Calls, email, LinkedIn | ✗ Depends on hire | ~ Email only (usually) |
| Reporting & visibility | ✓ Weekly transparent reports | ✗ Self-managed | ~ Varies by agency |
| No long-term lock-in | ✓ Month-to-month | N/A | ✗ Usually 6–12 mo contracts |
| Performance guarantee | ✓ 30-day money back | ✗ None | ✗ None |

Use `✓` icon for Leadzly positive values and `✗` icon (or `~` text for partial) for negative comparators.

---

### FAQ Accordion — `.faq` + `.faq__list` + `.faq__item`

The `.faq` section with its background and header is already scaffolded. Replace the placeholder paragraph.

**HTML structure:**

```
.faq
  .container
    .section-header
      .section-label     — "FAQ"
      .section-title     — "Common Questions, Honest Answers"
    .faq__list           — max-width: 720px; margin-inline: auto
      .faq__item (×7)    — border-bottom: 1px solid var(--color-border)
        button.faq__question  — full-width; text-left; flex row; justify-content: space-between
          span           — question text
          .faq__icon     — rotating chevron SVG (16×16)
        .faq__answer     — hidden by default; shown when item is active
          p              — answer body text
```

**Visual rules:**

| Element | Spec |
|---------|------|
| List container | `max-width: 720px; margin-inline: auto` |
| Item border | `border-bottom: 1px solid var(--color-border)` |
| Item active indicator | `border-left: 3px solid var(--color-primary)` on `.faq__item--active` |
| Question button | `width: 100%; padding: var(--space-4) 0; display: flex; align-items: center; justify-content: space-between; background: none; border: none; cursor: pointer; text-align: left` |
| Question text | `--text-lg`, weight 700, `var(--color-text)` |
| Chevron icon | 16×16 inline SVG, stroke `var(--color-text-muted)`, rotates 180° when active — `transform: rotate(180deg); transition: transform 200ms ease` |
| Answer container | `overflow: hidden; max-height: 0; transition: max-height 300ms cubic-bezier(0.16, 1, 0.3, 1)` — expanded to `max-height: 400px` when active |
| Answer text | `--text-sm`, weight 400, `var(--color-text-muted)`, line-height 1.6, `padding: 0 0 var(--space-4) 0` |
| Active question text | Remains `var(--color-text)` — no colour change on open |

**Interaction (vanilla JS — new `<script>` block or inline):**

- One question open at a time (accordion pattern — collapsing others on open)
- Toggle: click question button → toggle `.faq__item--active` on parent `.faq__item`, set `aria-expanded` on button, animate `max-height` on answer div
- Keyboard: button is a native `<button>` — Enter/Space handled natively
- Default state: all items closed on page load
- ARIA: `aria-expanded="false/true"` on button; answer div has `id` referenced by `aria-controls`; answer div has `role="region"` and `aria-labelledby` pointing to button

**7 questions — content specification:**

| # | Question | Answer |
|---|----------|--------|
| 1 | How quickly can you start generating meetings? | We typically onboard new clients within 30 days. Week one is ICP audit and script development; week two is outreach infrastructure setup; week three is live calling. Most clients see their first qualified meetings in weeks 3–4. |
| 2 | What does "qualified meeting" actually mean? | A qualified meeting is a booked appointment with a decision-maker who fits your ICP, understands what you do, and has agreed to the call. We don't count no-shows or rescheduled meetings that never happen. |
| 3 | Do I have to sign a long-term contract? | No. All plans are month-to-month. We don't believe in locking clients in — if we're not delivering, you should be free to leave. Our 30-day performance guarantee reflects that confidence. |
| 4 | What industries do you work with? | We've run campaigns across SaaS, recruitment, e-commerce, financial services, professional services, construction, managed IT, and more. If your product or service is sold B2B, we can build a campaign for it. |
| 5 | How is Leadzly different from a typical cold-calling agency? | Most agencies operate offshore and optimise for call volume. We're a UK-based team that optimises for meeting quality — every lead is qualified before it hits your calendar. You pay for results, not activity. |
| 6 | What information do you need to get started? | We need your ICP (ideal customer profile), your core value proposition, and access to your calendar or CRM. We handle the rest — list building, scripting, outreach, and qualification. |
| 7 | What happens if you don't hit the meeting targets? | Our 30-day guarantee means if we don't deliver qualified meetings in your first month, you don't pay for that month. We stand behind our results. |

---

### ROI Calculator — new section `.roi` inserted between FAQ and Booking

The ROI calculator is a **new section** not yet scaffolded in HTML. Insert it between `.faq` and `.booking`.

**HTML structure:**

```html
<section class="roi" id="roi">
  <div class="container">
    <div class="section-header">
      <span class="section-label">ROI Calculator</span>
      <h2 class="section-title">What Could Leadzly Be Worth to You?</h2>
      <p class="section-subtitle">Adjust the numbers below to see your projected pipeline uplift.</p>
    </div>
    <div class="roi__card">
      <div class="roi__inputs">
        <div class="roi__field">
          <label class="roi__label" for="roi-deal-value">Average deal value (£)</label>
          <input class="roi__input" type="number" id="roi-deal-value" min="0" step="500" value="5000" aria-describedby="roi-deal-value-hint">
          <span class="roi__hint" id="roi-deal-value-hint">The average value of a closed deal in your pipeline</span>
        </div>
        <div class="roi__field">
          <label class="roi__label" for="roi-current-meetings">Current qualified meetings/month</label>
          <input class="roi__input" type="number" id="roi-current-meetings" min="0" step="1" value="4" aria-describedby="roi-current-meetings-hint">
          <span class="roi__hint" id="roi-current-meetings-hint">How many qualified sales meetings you're booking today</span>
        </div>
        <div class="roi__field">
          <label class="roi__label" for="roi-target-meetings">Target meetings/month with Leadzly</label>
          <input class="roi__input" type="number" id="roi-target-meetings" min="0" step="1" value="20" aria-describedby="roi-target-meetings-hint">
          <span class="roi__hint" id="roi-target-meetings-hint">Realistic target — our Growth plan averages 20+ meetings/month</span>
        </div>
      </div>
      <div class="roi__outputs">
        <div class="roi__output">
          <span class="roi__output-label">Additional monthly pipeline</span>
          <span class="roi__output-value" id="roi-monthly">£80,000</span>
          <span class="roi__output-sub">Based on additional meetings × deal value × 1% close rate assumption</span>
        </div>
        <div class="roi__output">
          <span class="roi__output-label">Projected annual pipeline uplift</span>
          <span class="roi__output-value" id="roi-annual">£960,000</span>
          <span class="roi__output-sub">Monthly uplift × 12</span>
        </div>
      </div>
      <p class="roi__disclaimer">Estimates assume a 10% meeting-to-close conversion rate. Actual results vary. This calculator is for illustration only.</p>
    </div>
  </div>
</section>
```

**Visual rules:**

| Element | Spec |
|---------|------|
| Section background | `var(--section-tint)` — #f8fafb (maintains alternating rhythm: dark navy → white → tint) |
| `.roi__card` | background `var(--color-bg)` (#ffffff), border `1px solid var(--color-border)`, border-radius `var(--radius-xl)`, padding `var(--space-8)`, box-shadow `var(--shadow-md)`, max-width 800px, margin-inline auto |
| `.roi__inputs` | CSS grid, `grid-template-columns: 1fr` (mobile), `repeat(3, 1fr)` at ≥768px, gap `var(--space-6)` |
| `.roi__label` | `--text-sm`, weight 700, `var(--color-text)`, display block, margin-bottom `var(--space-2)` |
| `.roi__input` | width 100%, padding `var(--space-3) var(--space-4)`, border `1px solid var(--color-border)`, border-radius `var(--radius-md)`, font-size `var(--text-sm)`, color `var(--color-text)`, background `var(--color-bg)` |
| `.roi__input:focus` | outline `2px solid var(--color-primary)`, outline-offset 2px (inherits from `base.css :focus-visible`) |
| `.roi__hint` | `--text-xs`, weight 400, `var(--color-text-faint)`, display block, margin-top `var(--space-2)` |
| `.roi__outputs` | CSS grid, `grid-template-columns: 1fr` (mobile), `repeat(2, 1fr)` at ≥640px, gap `var(--space-6)`, margin-top `var(--space-8)` |
| `.roi__output` | background `var(--color-primary-highlight)`, border `1px solid rgba(22,163,74,0.2)`, border-radius `var(--radius-lg)`, padding `var(--space-6)`, text-align center |
| `.roi__output-label` | `--text-xs`, weight 700, `var(--color-primary)`, text-transform uppercase, letter-spacing 0.06em, display block, margin-bottom `var(--space-3)` |
| `.roi__output-value` | `--text-xl`, weight 700, `var(--color-primary)`, display block, font-family `var(--font-display)` |
| `.roi__output-sub` | `--text-xs`, weight 400, `var(--color-text-muted)`, display block, margin-top `var(--space-2)` |
| `.roi__disclaimer` | `--text-xs`, weight 400, `var(--color-text-faint)`, text-align center, margin-top `var(--space-6)`, font-style italic |

**Calculation logic (client-side JS only — no server call):**

```
additionalMeetings = targetMeetings - currentMeetings
additionalMeetings = Math.max(0, additionalMeetings)

CONVERSION_RATE = 0.10   // 10% meeting-to-close assumption

additionalMonthlyRevenue = additionalMeetings × dealValue × CONVERSION_RATE
annualPipeline = additionalMonthlyRevenue × 12

Display formatted as: £{value.toLocaleString('en-GB')}
```

- Recalculate on every `input` event (real-time, no submit button)
- If any input is empty or negative, display `£0` for both outputs
- Output element IDs: `#roi-monthly`, `#roi-annual`
- No form submission, no fetch, no external calls

---

### Eyebrow Label Audit — DSGN-01

The following sections are missing `.section-label` eyebrow spans or need one added. Executor audits and adds as needed:

| Section | Current Eyebrow | Required Eyebrow | Action |
|---------|----------------|------------------|--------|
| Hero | Existing `.hero__eyebrow` pill badge | Existing pattern is acceptable — pill format retained | No change |
| Pain Points | "Sound Familiar?" | Confirm present | Verify in HTML |
| Services | "What We Do" | Confirm present | Verify in HTML |
| About | "About Us" | Confirm present | Verify in HTML |
| Multi-Channel | "Our Approach" | Confirm present | Verify in HTML |
| How It Works | "Our Process" | Confirm present | Verify in HTML |
| Industries | "Industries" | Confirm present | Verify in HTML |
| Proven | "Results" | Confirm present | Verify in HTML |
| Comparison | "Why Leadzly" | Present in scaffold | Confirm |
| Pricing | "Pricing" | Confirm present | Verify in HTML |
| FAQ | "FAQ" | Present in scaffold | Confirm |
| ROI Calculator | "ROI Calculator" | New — add | Add in new section HTML |
| Booking | "Book a Call" | Present in scaffold | Confirm |

All eyebrow labels: `--text-xs`, weight 700, `var(--color-primary)`, `text-transform: uppercase`, `letter-spacing: 0.08em`, `display: inline-block`, `margin-bottom: var(--space-3)`.

---

## Interaction Contract

| Element | Trigger | Behaviour | Implementation |
|---------|---------|-----------|----------------|
| FAQ accordion | Click question button | Toggle active item; collapse others; animate answer height via `max-height` transition | Vanilla JS — new `<script>` block in `index.html` or separate `page.js` file |
| FAQ chevron | FAQ item active state | Rotate 180° | CSS `transform: rotate(180deg)` on `.faq__item--active .faq__icon` |
| ROI inputs | `input` event on any field | Recalculate and update both output values in real time | Vanilla JS — `addEventListener('input', calculate)` on each input |
| Comparison table | Mobile horizontal scroll | Table scrolls horizontally inside `.comparison__table-wrap` | CSS `overflow-x: auto` on wrapper |
| Hero h1 | Static — no interaction | N/A | CSS `font-weight: 800; font-size: clamp(3.5rem, 3.5vw + 0.5rem, 4.75rem)` update |

**JS delivery:** Add a new `<script>` block before `</body>` in `index.html` (or extract to `page.js` linked at end of body). Keep as IIFE to avoid global scope pollution. No libraries. No build step.

**Keyboard accessibility:**
- FAQ buttons are native `<button>` elements — Enter/Space work without custom JS
- ROI inputs are native `<input type="number">` — keyboard navigation is native
- `aria-expanded` toggled on FAQ buttons
- `aria-controls` on FAQ buttons pointing to answer region IDs

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA for this phase | "Book a Strategy Call" (no new CTA buttons added in Phase 4 — CTAs are a Phase 5 concern) |
| Comparison section eyebrow | "Why Leadzly" |
| Comparison section title | "How We Stack Up" |
| Comparison section subtitle | "See how Leadzly compares to building in-house or working with a typical agency." |
| Comparison column headers | "Leadzly" / "In-House" / "Other Agencies" |
| FAQ section eyebrow | "FAQ" |
| FAQ section title | "Common Questions, Honest Answers" |
| ROI section eyebrow | "ROI Calculator" |
| ROI section title | "What Could Leadzly Be Worth to You?" |
| ROI section subtitle | "Adjust the numbers below to see your projected pipeline uplift." |
| ROI disclaimer | "Estimates assume a 10% meeting-to-close conversion rate. Actual results vary. This calculator is for illustration only." |
| ROI output label 1 | "Additional monthly pipeline" |
| ROI output label 2 | "Projected annual pipeline uplift" |
| ROI sub-label 1 | "Based on additional meetings × deal value × 10% close rate assumption" |
| ROI sub-label 2 | "Monthly uplift × 12" |
| Empty state (ROI at zero) | Display "£0" — no separate empty state message needed; the disclaimer explains the model |
| Error state (ROI invalid input) | Clamp negative inputs to zero silently — no error message shown; invalid inputs simply produce "£0" output |
| Destructive actions | None — this phase contains no user data deletion, no form submission, no irreversible actions |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn |
| Any third-party | none | not applicable — vanilla HTML/CSS stack |

No external script dependencies added in this phase. All JS is inline vanilla. No CDN additions beyond Calendly (already present, not modified in this phase).

---

## Section Order After Phase 4

After this phase completes, the page section order must be:

1. Hero (h1 updated — DSGN-01)
2. Trust Bar
3. Pain Points
4. Services
5. About
6. Multi-Channel
7. How It Works
8. Industries
9. Proven Across Industries
10. **Comparison Table** (filled — SECT-01)
11. Pricing
12. **FAQ** (filled — SECT-02)
13. **ROI Calculator** (new — SECT-03, inserted between FAQ and Booking)
14. Booking (scaffold — Phase 5)
15. Footer CTA
16. Footer

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: 04-conversion-sections*
*UI-SPEC created: 2026-04-27*
*UI-SPEC revised: 2026-04-27 — fixed --text-base → var(--text-sm) (Issue 1); added design system exception notes for --space-3, --space-10, --space-24 (Issue 2)*
*Sources: REQUIREMENTS.md (SECT-01, SECT-02, SECT-03, DSGN-01), ROADMAP.md Phase 4 success criteria, STATE.md accumulated context, style.css (all design tokens), page.css (component patterns), base.css (typography base), 03-UI-SPEC.md (typography and spacing conventions carried forward)*

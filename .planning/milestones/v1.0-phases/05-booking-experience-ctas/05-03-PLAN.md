---
phase: 05-booking-experience-ctas
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - index.html
  - page.css
autonomous: true
requirements:
  - BOOK-03

must_haves:
  truths:
    - "The hero primary CTA reads 'Book a Free Strategy Call'"
    - "The hero secondary CTA reads 'See If We're a Fit' and links to #booking"
    - "All three pricing card CTAs read 'Start Filling Your Calendar'"
    - "The ROI calculator section has a 'Get Your Meetings Forecast' CTA button linking to #booking"
    - "At least 4 distinct CTA copy variants are present across the page"
  artifacts:
    - path: "index.html"
      provides: "Updated hero CTA copy"
      contains: "Book a Free Strategy Call"
    - path: "index.html"
      provides: "Updated hero secondary CTA copy and href"
      contains: "See If We're a Fit"
    - path: "index.html"
      provides: "Updated pricing CTAs"
      contains: "Start Filling Your Calendar"
    - path: "index.html"
      provides: "New ROI section CTA button"
      contains: "Get Your Meetings Forecast"
    - path: "page.css"
      provides: "ROI CTA layout styles"
      contains: ".roi__cta"
  key_links:
    - from: "Hero secondary CTA"
      to: "#booking section"
      via: "href attribute"
      pattern: "href=\"#booking\""
    - from: "ROI CTA button"
      to: "#booking section"
      via: "href attribute"
      pattern: "href=\"#booking\""
---

<objective>
Update CTA copy across the page to create at least 4 distinct conversion messages. Replace generic "Get Started" and "Book a Call" copy. Add a missing CTA button to the ROI calculator section.

Purpose: Varied CTA copy reduces banner blindness and matches visitor intent at different stages of the page — reducing friction at each conversion touch point.
Output: 4 distinct CTA labels visible across the page; ROI section has its own CTA; hero secondary CTA sends visitors directly to the booking section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-booking-experience-ctas/05-UI-SPEC.md
@.planning/phases/05-booking-experience-ctas/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update hero CTAs and all three pricing card CTAs</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 154–157 for hero CTAs; lines 652–684 for pricing card CTAs)
  </read_first>
  <action>
Make these exact text and attribute replacements in index.html:

**Hero primary CTA (line 155):**
Find:
```html
          <a href="https://calendly.com/allan-chan-leadzly" target="_blank" rel="noopener noreferrer" class="btn btn--primary btn--lg">Book a Strategy Call <span aria-hidden="true">→</span></a>
```
Replace with:
```html
          <a href="https://calendly.com/allan-chan-leadzly" target="_blank" rel="noopener noreferrer" class="btn btn--primary btn--lg">Book a Free Strategy Call <span aria-hidden="true">→</span></a>
```

**Hero secondary CTA (line 156):**
Find:
```html
          <a href="#services" class="btn btn--outline btn--lg">How It Works</a>
```
Replace with:
```html
          <a href="#booking" class="btn btn--outline btn--lg">See If We're a Fit</a>
```

**Pricing Starter CTA (line 654):**
Find:
```html
          <a href="https://calendly.com/allan-chan-leadzly" target="_blank" rel="noopener noreferrer" class="btn btn--outline btn--full">Get Started</a>
```
Replace with:
```html
          <a href="https://calendly.com/allan-chan-leadzly" target="_blank" rel="noopener noreferrer" class="btn btn--outline btn--full">Start Filling Your Calendar</a>
```

**Pricing Growth CTA (line 669):**
Find:
```html
          <a href="https://calendly.com/allan-chan-leadzly" target="_blank" rel="noopener noreferrer" class="btn btn--primary btn--full">Get Started</a>
```
Replace with:
```html
          <a href="https://calendly.com/allan-chan-leadzly" target="_blank" rel="noopener noreferrer" class="btn btn--primary btn--full">Start Filling Your Calendar</a>
```

**Pricing Enterprise CTA (line 683):**
Find:
```html
          <a href="https://calendly.com/allan-chan-leadzly" target="_blank" rel="noopener noreferrer" class="btn btn--outline btn--full">Book a Call</a>
```
Replace with:
```html
          <a href="https://calendly.com/allan-chan-leadzly" target="_blank" rel="noopener noreferrer" class="btn btn--outline btn--full">Start Filling Your Calendar</a>
```
  </action>
  <verify>
    <automated>grep -c "Book a Free Strategy Call" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `Book a Free Strategy Call` (hero primary CTA)
    - index.html contains `See If We're a Fit` (hero secondary CTA)
    - index.html does NOT contain `>How It Works<` (old secondary CTA copy removed)
    - index.html contains exactly 3 instances of `Start Filling Your Calendar` (one per pricing card)
    - index.html does NOT contain `>Get Started<` (all generic pricing CTAs replaced)
    - The "See If We're a Fit" anchor has `href="#booking"` (not `href="#services"`)
  </acceptance_criteria>
  <done>Hero CTAs and all three pricing card CTAs show updated copy in browser; secondary hero CTA scrolls to #booking section when clicked.</done>
</task>

<task type="auto">
  <name>Task 2: Add "Get Your Meetings Forecast" CTA to ROI calculator section</name>
  <files>index.html, page.css</files>
  <read_first>
    - index.html (lines 810–815 — ROI section ending with .roi__disclaimer paragraph)
    - page.css (last 5 lines — confirm end of file to append .roi__cta)
  </read_first>
  <action>
In the ROI calculator section, find the `.roi__disclaimer` paragraph and the closing `</div>` of `.roi__card` (lines 812–814):
```html
        <p class="roi__disclaimer">Estimates assume a 10% meeting-to-close conversion rate. Actual results vary. This calculator is for illustration only.</p>
      </div>
    </div>
  </section>
```

Replace with:
```html
        <p class="roi__disclaimer">Estimates assume a 10% meeting-to-close conversion rate. Actual results vary. This calculator is for illustration only.</p>
        <div class="roi__cta">
          <a href="#booking" class="btn btn--primary">Get Your Meetings Forecast <span aria-hidden="true">→</span></a>
        </div>
      </div>
    </div>
  </section>
```

Then append the following CSS to the end of `page.css`:
```css
.roi__cta {
  text-align: center;
  margin-top: var(--space-8);
}
```
  </action>
  <verify>
    <automated>grep -c "Get Your Meetings Forecast" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `Get Your Meetings Forecast`
    - index.html contains `class="roi__cta"` wrapping the new CTA anchor
    - The "Get Your Meetings Forecast" anchor has `href="#booking"`
    - page.css contains `.roi__cta` with `text-align: center`
    - The CTA appears after `.roi__disclaimer` and before the closing `</div>` of `.roi__card`
  </acceptance_criteria>
  <done>ROI section shows a centred "Get Your Meetings Forecast" button below the disclaimer; clicking it scrolls to the booking section.</done>
</task>

</tasks>

<verification>
grep "Book a Free Strategy Call" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'
grep "See If We're a Fit" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'
grep "Start Filling Your Calendar" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'
grep "Get Your Meetings Forecast" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'
grep "roi__cta" '/c/Work File/Elevateo Work/Leadzly - Company/page.css'
</verification>

<success_criteria>
1. At least 4 distinct CTA copy variants present: "Book a Free Strategy Call", "See If We're a Fit", "Start Filling Your Calendar", "Get Your Meetings Forecast"
2. Hero secondary CTA "See If We're a Fit" scrolls directly to #booking on click
3. ROI section has a visible centred CTA button below the disclaimer
4. No "Get Started" or "How It Works" copy remains in CTA buttons
</success_criteria>

<output>
After completion, create `.planning/phases/05-booking-experience-ctas/05-03-SUMMARY.md`
</output>

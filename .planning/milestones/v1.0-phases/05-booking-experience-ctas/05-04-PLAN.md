---
phase: 05-booking-experience-ctas
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - index.html
autonomous: false
requirements:
  - QWIN-03

must_haves:
  truths:
    - "Hero stat numbers visibly count up from zero when the hero card scrolls into view"
    - "The animation plays exactly once per page load — re-scrolling does not replay it"
    - "If IntersectionObserver is not supported, stat numbers display their final values immediately (graceful fallback)"
  artifacts:
    - path: "index.html"
      provides: "data-count-to attributes on all three .hero__card-stat-num spans"
      contains: "data-count-to"
    - path: "index.html"
      provides: "JS IIFE using IntersectionObserver + requestAnimationFrame for counter animation"
      contains: "requestAnimationFrame"
  key_links:
    - from: "IntersectionObserver watching .hero__card"
      to: "[data-count-to] spans"
      via: "querySelectorAll + requestAnimationFrame step loop"
      pattern: "data-count-to"
---

<objective>
Add data attributes to the three hero stat number spans and implement a JS IIFE that uses IntersectionObserver to trigger a count-up animation (0 → target over 1200ms, ease-out cubic) when the hero card scrolls into view for the first time. Followed by a human verify checkpoint covering all Phase 5 deliverables.

Purpose: Animated stats add dynamism to the hero card and signal live, real-time performance data to new visitors.
Output: Hero stats animate from 0 to 12, 94%, and 8 on first scroll-into-view; then a human verify checkpoint for the full phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-booking-experience-ctas/05-UI-SPEC.md
@.planning/phases/05-booking-experience-ctas/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-count-to attributes to hero stat spans and add counter JS IIFE</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 189–203 for .hero__card-stats spans; lines 1060–1068 for end of file / existing script blocks)
  </read_first>
  <action>
**Step A — Add data attributes to hero stat spans.**

Find the three `.hero__card-stat-num` spans (lines ~191–201). Make these exact replacements:

Stat 1 — currently: `<span class="hero__card-stat-num">12</span>`
Replace with: `<span class="hero__card-stat-num" data-count-to="12">12</span>`

Stat 2 — currently: `<span class="hero__card-stat-num">94%</span>`
Replace with: `<span class="hero__card-stat-num" data-count-to="94" data-count-suffix="%">94%</span>`

Stat 3 — currently: `<span class="hero__card-stat-num">8</span>`
Replace with: `<span class="hero__card-stat-num" data-count-to="8">8</span>`

**Step B — Add stat counter IIFE.**
After the floating CTA script block (still before `</body>`), add a new `<script>` block:

```html
  <script>
  // Hero stat counter — count up on first scroll into view (QWIN-03)
  (function() {
    var card = document.querySelector('.hero__card');
    if (!card || !('IntersectionObserver' in window)) return;
    var animated = false;
    var observer = new IntersectionObserver(function(entries) {
      if (animated || !entries[0].isIntersecting) return;
      animated = true;
      observer.disconnect();
      var nums = card.querySelectorAll('[data-count-to]');
      nums.forEach(function(el) {
        var target = parseInt(el.dataset.countTo, 10);
        var suffix = el.dataset.countSuffix || '';
        var duration = 1200;
        var start = null;
        function step(ts) {
          if (!start) start = ts;
          var progress = Math.min((ts - start) / duration, 1);
          var eased = 1 - Math.pow(1 - progress, 3);
          el.textContent = Math.round(eased * target) + suffix;
          if (progress < 1) requestAnimationFrame(step);
        }
        requestAnimationFrame(step);
      });
    }, { threshold: 0.5 });
    observer.observe(card);
  })();
  </script>
```
  </action>
  <verify>
    <automated>grep -c "data-count-to" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains exactly 3 instances of `data-count-to`
    - index.html contains `data-count-to="12"` on a `.hero__card-stat-num` span
    - index.html contains `data-count-to="94" data-count-suffix="%"` on a `.hero__card-stat-num` span
    - index.html contains `data-count-to="8"` on a `.hero__card-stat-num` span
    - index.html contains `requestAnimationFrame` inside the counter IIFE
    - index.html contains `observer.disconnect()` inside the counter IIFE (one-shot guard)
    - index.html contains `Math.pow(1 - progress, 3)` (ease-out cubic formula)
  </acceptance_criteria>
  <done>Data attributes present on all three stat spans; counter IIFE script block present; browser opens and stats count up from 0 on first load.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — all Phase 5 deliverables</name>
  <what-built>
    Complete Phase 5: Booking Experience &amp; CTAs. Four deliverables across 4 plans:
    1. (Plan 01) Inline Calendly embed — two-column booking section with benefit list left and live calendar right
    2. (Plan 02) Floating sticky CTA — bottom-right, pulsing dot, slides in after 3s, hidden on mobile
    3. (Plan 03) CTA copy variants — "Book a Free Strategy Call", "See If We're a Fit", "Start Filling Your Calendar", "Get Your Meetings Forecast"
    4. (Plan 04) Hero stat counter animation — numbers count up from 0 on first scroll
  </what-built>
  <how-to-verify>
    Open `index.html` in Chrome (or any browser). Run through each check:

    **BOOK-01 — Booking section**
    1. Scroll to the "Ready to Fill Your Calendar?" section
    2. Confirm two-column layout: benefit list on the left, Calendly calendar on the right
    3. Confirm the Calendly calendar loads and shows a month view — no internal scrollbar
    4. Confirm 5 benefit bullets are visible with green checkmark icons
    5. Resize to 375px width — confirm layout stacks to single column (benefits above, calendar below)

    **BOOK-02 — Floating sticky CTA**
    6. Reload the page — confirm bottom-right corner is empty for the first 3 seconds
    7. After 3 seconds, confirm "Book a Call" button slides up into view with a pulsing white dot
    8. Resize to 375px width — confirm floating button is not visible

    **BOOK-03 — CTA copy variants**
    9. Confirm hero primary CTA reads "Book a Free Strategy Call"
    10. Confirm hero secondary CTA reads "See If We're a Fit" — click it and confirm it scrolls to the booking section
    11. Confirm all three pricing cards read "Start Filling Your Calendar"
    12. Confirm the ROI calculator section has a "Get Your Meetings Forecast" button below the disclaimer

    **QWIN-03 — Hero stat counter**
    13. Reload the page with a hard refresh (Ctrl+Shift+R)
    14. Scroll the hero stats card into view — confirm the three numbers count up from 0: "12", "94%", "8"
    15. Scroll away and back — confirm the animation does NOT replay (numbers stay at final values)

    **Cookie banner + floating CTA coexistence (desktop)**
    16. Clear localStorage (DevTools → Application → Local Storage → delete `leadzly_cookie_consent`)
    17. Reload — confirm the cookie banner appears AND the floating CTA is visible after 3s — confirm the cookie banner accept button remains clickable
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
grep "data-count-to" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'
grep "requestAnimationFrame" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'
grep "observer.disconnect" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'
</verification>

<success_criteria>
1. Hero stats count up from 0 when scrolled into view — animation duration ~1.2 seconds, ease-out curve
2. Animation plays once per page load — no replay on re-scroll
3. All Phase 5 success criteria verified by human: two-column booking layout, floating CTA with 3s delay and pulsing dot, 4 distinct CTA copy variants, stat counter animation
4. Human approves checkpoint — Phase 5 complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-booking-experience-ctas/05-04-SUMMARY.md`
</output>

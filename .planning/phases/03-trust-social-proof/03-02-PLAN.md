---
phase: 03-trust-social-proof
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - index.html
  - page.css
autonomous: true
requirements:
  - TRST-03
  - TRST-04

must_haves:
  truths:
    - "A 'Proven Across Industries' section is visible at the #case-studies anchor with 4 industry cards"
    - "Each card shows a green industry tag chip, a bold one-line outcome, and a muted detail line"
    - "The aggregate stat '100+ meetings booked monthly' appears as the section subtitle"
    - "No testimonials section appears anywhere on the page"
    - "The #case-studies nav anchor still works (section has id='case-studies')"
  artifacts:
    - path: "index.html"
      provides: "Proven Across Industries section HTML replacing case-studies scaffold"
      contains: "class=\"proven\""
    - path: "page.css"
      provides: "Proven section CSS + testimonials CSS removed"
      contains: ".proven {"
  key_links:
    - from: "index.html .proven section"
      to: "nav href='#case-studies' and footer href='#case-studies'"
      via: "id='case-studies' attribute preserved on .proven section"
      pattern: "id=\"case-studies\""
    - from: ".proven__card-tag elements"
      to: ".proven__card-tag CSS rule in page.css"
      via: "class attribute"
      pattern: "proven__card-tag"
---

<objective>
Replace the `.case-studies` scaffold section with a fully-built "Proven Across Industries" section containing 4 industry cards, then delete the `.testimonials` scaffold section and its dead CSS.

Purpose: TRST-03 and TRST-04 — the "Proven Across Industries" section is the primary evidence block for Phase 3. It uses real portfolio verticals and a genuine aggregate stat to displace scepticism without fabricating case studies or metrics.

Output: `.proven` section in index.html (id="case-studies" preserved) + `.proven` CSS in page.css + `.testimonials` HTML and CSS deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-trust-social-proof/03-CONTEXT.md
@.planning/phases/03-trust-social-proof/03-UI-SPEC.md
@.planning/phases/03-trust-social-proof/03-RESEARCH.md
@.planning/phases/03-trust-social-proof/03-01-SUMMARY.md

<interfaces>
<!-- Key patterns from existing codebase — use these directly. -->

Existing section structure to REPLACE (index.html lines 448–460):
```html
  <!-- ============ CASE STUDIES ============ -->
  <section class="case-studies" id="case-studies">
    <div class="container">
      <div class="section-header">
        <span class="section-label">Case Studies</span>
        <h2 class="section-title">Real Results, Coming Soon</h2>
        ...
      </div>
      <div class="case-studies__cta">...</div>
    </div>
  </section>
```

Testimonials section to DELETE (index.html lines 462–473):
```html
  <!-- ============ TESTIMONIALS (scaffold — content in Phase 3) ============ -->
  <section class="testimonials" id="testimonials">
    ...
    <p class="scaffold-placeholder">Testimonial cards coming in Phase 3.</p>
  </section>
```

CSS to DELETE from page.css (lines 1152–1155):
```css
.testimonials {
  padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;
  background: var(--section-tint);
}
```

CSS to REPLACE in page.css (lines 1172–1181 — the .case-studies block):
```css
/* Case Studies */
.case-studies {
  padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;
  background: var(--section-white);
}
.case-studies__cta {
  display: flex;
  justify-content: center;
  margin-top: var(--space-8);
}
```

Confirmed CSS tokens (from style.css :root):
- `--section-white`        — proven section background (D-06)
- `--color-surface`        — card background (#f9fafb)
- `--color-border`         — card border
- `--color-primary-soft`   — tag chip background (#f0fdf4)
- `--color-primary`        — tag chip text (#16a34a)
- `--color-text`           — card outcome heading
- `--color-text-muted`     — card detail line
- `--radius-lg`            — card border-radius
- `--radius-full`          — tag chip border-radius
- `--shadow-sm`            — card box-shadow
- `--space-1`, `--space-2`, `--space-3`, `--space-6`, `--space-12`, `--space-24`
- `--text-xs`, `--text-sm`, `--text-lg`
- `--font-display`         — Plus Jakarta Sans (card outcome heading)
- `--font-body`            — Inter (tag chip, detail line)

Shared classes — DO NOT MODIFY:
- `.section-header`, `.section-label`, `.section-title`, `.section-subtitle`
- `.fade-in` — add to cards for Phase 6 animation hooks
- `.container`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace case-studies scaffold with Proven Across Industries section</name>
  <files>index.html</files>
  <read_first>
    - `index.html` lines 448–475 — read the exact case-studies AND testimonials section HTML to understand what is being removed (note exact line numbers may have shifted slightly after Plan 01's trust bar insertion)
    - Verify that `id="case-studies"` currently exists on the case-studies section open tag — this ID must be preserved on the new .proven section
  </read_first>
  <action>
**Step 1 — Replace the entire `.case-studies` section**

Find and replace the entire block from `<!-- ============ CASE STUDIES ============ -->` through the closing `</section>` of that section. Replace it with:

```html
  <!-- ============ PROVEN ACROSS INDUSTRIES ============ -->
  <section class="proven" id="case-studies">
    <div class="container">
      <div class="section-header">
        <span class="section-label">Results</span>
        <h2 class="section-title">Proven Across Industries</h2>
        <p class="section-subtitle">100+ meetings booked monthly across the Elevateo portfolio</p>
      </div>
      <div class="proven__grid">
        <div class="proven__card fade-in">
          <span class="proven__card-tag">Digital Marketing</span>
          <p class="proven__card-outcome">Consistent pipeline for a growing agency</p>
          <p class="proven__card-detail">Outbound calling + email sequences targeting UK SME decision-makers</p>
        </div>
        <div class="proven__card fade-in">
          <span class="proven__card-tag">AI &amp; Automation</span>
          <p class="proven__card-outcome">Early-stage SaaS demos booked at scale</p>
          <p class="proven__card-detail">LinkedIn prospecting + follow-up cadences for a pre-revenue product</p>
        </div>
        <div class="proven__card fade-in">
          <span class="proven__card-tag">SaaS &amp; Software</span>
          <p class="proven__card-outcome">Qualified discovery calls without an internal SDR</p>
          <p class="proven__card-detail">Multi-channel outreach across cold email and telesales</p>
        </div>
        <div class="proven__card fade-in">
          <span class="proven__card-tag">E-Commerce</span>
          <p class="proven__card-outcome">New B2B retail partnerships opened monthly</p>
          <p class="proven__card-detail">Targeted cold outreach to buyers at independent and chain retailers</p>
        </div>
      </div>
    </div>
  </section>
```

**Critical checks:**
- The new section has `class="proven"` AND `id="case-studies"` — both attributes required
- The section comment says "PROVEN ACROSS INDUSTRIES" (rename from CASE STUDIES)
- AI & Automation and SaaS & Software use `&amp;` not `&` in HTML

**Step 2 — Delete the entire `.testimonials` section**

Find and delete the entire block from `<!-- ============ TESTIMONIALS` comment through the closing `</section>` of that section (currently after the proven section). The block to remove:

```html
  <!-- ============ TESTIMONIALS (scaffold — content in Phase 3) ============ -->
  <section class="testimonials" id="testimonials">
    <div class="container">
      <div class="section-header">
        <span class="section-label">Testimonials</span>
        <h2 class="section-title">What Our Clients Say</h2>
        <p class="section-subtitle">Real results from real businesses across the UK.</p>
      </div>
      <!-- Cards added in Phase 3 (TRST-01, TRST-02) -->
      <p class="scaffold-placeholder">Testimonial cards coming in Phase 3.</p>
    </div>
  </section>
```

Delete this block entirely. There must be no `.testimonials` section in the HTML after this task.
  </action>
  <verify>
    <automated>grep -n "class=\"proven\"\|id=\"case-studies\"\|class=\"testimonials\"" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class=\"proven\"" index.html` returns 1
    - `grep "id=\"case-studies\"" index.html` returns exactly 1 match (on the .proven section open tag)
    - `grep "class=\"testimonials\"" index.html` returns 0 matches (section fully deleted)
    - `grep "scaffold-placeholder" index.html` returns 0 matches for "Testimonial cards" (testimonials placeholder gone)
    - `grep "Proven Across Industries" index.html` returns 1 match
    - `grep "100+ meetings booked monthly" index.html` returns 1 match (section subtitle)
    - `grep -c "proven__card" index.html` returns at least 4 (one per card; each card has multiple class uses so count may be higher)
    - `grep "Digital Marketing" index.html` returns 1 match inside a proven__card-tag span
    - `grep "AI &amp; Automation" index.html` returns 1 match (HTML entity encoded correctly)
  </acceptance_criteria>
  <done>The .proven section with id="case-studies" exists in index.html with 4 cards. The .testimonials section is fully removed. No scaffold-placeholder text remains for testimonials.</done>
</task>

<task type="auto">
  <name>Task 2: Add proven CSS and remove dead testimonials CSS</name>
  <files>page.css</files>
  <read_first>
    - `page.css` lines 1148–1185 — read the "SCAFFOLD SECTIONS" block to see the exact `.testimonials` CSS and `.case-studies` CSS that need to be replaced/removed
    - Confirm line numbers are approximately correct (may have shifted if earlier phases added lines)
  </read_first>
  <action>
**Step 1 — Remove the `.testimonials` CSS block**

Find and delete the following block from page.css (in the SCAFFOLD SECTIONS area):

```css
.testimonials {
  padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;
  background: var(--section-tint);
}
```

Delete only this block. Leave `.comparison`, `.faq`, `.booking` CSS intact.

**Step 2 — Replace the `.case-studies` CSS block**

Find and replace the existing case-studies CSS block:
```css
/* Case Studies */
.case-studies {
  padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;
  background: var(--section-white);
}
.case-studies__cta {
  display: flex;
  justify-content: center;
  margin-top: var(--space-8);
}
```

Replace it with the complete `.proven` CSS:

```css
/* ============ PROVEN ACROSS INDUSTRIES ============ */
.proven {
  padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;
  background: var(--section-white);
}
.proven__grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--space-6);
}
@media (min-width: 640px) {
  .proven__grid { grid-template-columns: repeat(2, 1fr); }
}
.proven__card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-6);
  box-shadow: var(--shadow-sm);
}
.proven__card-tag {
  display: inline-flex;
  align-items: center;
  background: var(--color-primary-soft);
  color: var(--color-primary);
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 700;
  border-radius: var(--radius-full);
  padding: var(--space-1) var(--space-3);
  margin-bottom: var(--space-3);
}
.proven__card-outcome {
  font-family: var(--font-display);
  font-size: var(--text-lg);
  font-weight: 700;
  color: var(--color-text);
  line-height: 1.3;
  margin-bottom: var(--space-2);
}
.proven__card-detail {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  font-weight: 400;
  color: var(--color-text-muted);
  line-height: 1.6;
}
```

**Anti-patterns to avoid:**
- Do NOT write any hex values — use CSS custom property tokens only
- Do NOT modify `.section-header`, `.section-label`, `.section-title`, or `.section-subtitle`
- Do NOT touch `.comparison`, `.faq`, `.booking` CSS blocks
  </action>
  <verify>
    <automated>grep -n "\.testimonials\|\.case-studies\|\.proven {" '/c/Work File/Elevateo Work/Leadzly - Company/page.css'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.testimonials" page.css` returns 0 (dead CSS block fully removed)
    - `grep -c "\.case-studies" page.css` returns 0 (old case-studies CSS replaced)
    - `grep "\.proven {" page.css` returns 1 match
    - `grep "\.proven__card {" page.css` returns 1 match
    - `grep "\.proven__card-tag {" page.css` returns 1 match
    - `grep "\.proven__card-outcome {" page.css` returns 1 match
    - `grep "\.proven__card-detail {" page.css` returns 1 match
    - `grep "var(--section-white)" page.css` returns a match inside `.proven {` (not hardcoded #ffffff)
    - `grep "var(--color-primary-soft)" page.css` returns a match inside `.proven__card-tag`
    - `grep "grid-template-columns: repeat(2, 1fr)" page.css` returns a match (2-column breakpoint)
    - `grep "#" page.css | grep "proven"` returns 0 matches (no hardcoded hex in proven CSS)
  </acceptance_criteria>
  <done>The .proven CSS block exists in page.css with all child rules. The .testimonials CSS block is gone. The .case-studies CSS block is gone. No hardcoded hex values in the new CSS.</done>
</task>

</tasks>

<verification>
After both tasks complete, open index.html in a browser and verify:
1. Scrolling to the #case-studies anchor (click "Our Work" or "Results" in nav) lands on a section titled "Proven Across Industries"
2. Four cards are visible in a 2×2 grid on desktop: Digital Marketing, AI & Automation, SaaS & Software, E-Commerce
3. Each card shows a green rounded chip tag, a bold outcome line, and a muted grey detail line
4. The section subtitle reads "100+ meetings booked monthly across the Elevateo portfolio"
5. No testimonials section appears anywhere while scrolling the full page
6. Background is white (consistent with surrounding sections)
</verification>

<success_criteria>
- `grep -c "class=\"proven\"" index.html` returns 1
- `grep "id=\"case-studies\"" index.html` returns 1 on the proven section
- `grep -c "class=\"testimonials\"" index.html` returns 0
- `grep "\.proven {" page.css` returns 1
- `grep "\.testimonials" page.css` returns 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-trust-social-proof/03-02-SUMMARY.md` following the summary template.
</output>

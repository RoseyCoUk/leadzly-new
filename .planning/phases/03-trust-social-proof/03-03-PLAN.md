---
phase: 03-trust-social-proof
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - index.html
autonomous: false
requirements:
  - TRST-06

must_haves:
  truths:
    - "The About section appears between the Services section and the Proven Across Industries section when scrolling the page"
    - "Clicking the nav link '#about' scrolls to the repositioned About section correctly"
    - "The About section content is unchanged — NI-based, no-offshore messaging is intact"
    - "The About section opening tag retains id='about' after the cut-paste operation"
  artifacts:
    - path: "index.html"
      provides: "About section in new position (after #services, before #case-studies)"
      contains: "id=\"about\""
  key_links:
    - from: "nav href='#about' and footer href='#about'"
      to: "repositioned .about section"
      via: "id='about' attribute on section element"
      pattern: "id=\"about\""
---

<objective>
Cut the About section from its current position (after pricing, line ~545) and paste it between the Services section and the Proven Across Industries section — no content changes, HTML repositioning only.

Purpose: TRST-06 — moving About earlier in the page flow places the "who we are + NI-based, no-offshore" narrative just before the evidence sections, reinforcing credibility before the reader encounters proof points.

Output: index.html with About section in the new position. No CSS changes. No content changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-trust-social-proof/03-CONTEXT.md
@.planning/phases/03-trust-social-proof/03-UI-SPEC.md
@.planning/phases/03-trust-social-proof/03-02-SUMMARY.md

<interfaces>
<!-- Key patterns from existing codebase — use these directly. -->

After Plans 01 and 02, the current section order in index.html is:
1. Hero (#hero)
2. Trust Bar (.trust-bar) — inserted by Plan 01
3. Pain Points (#pain-points)
4. Services (#services)
5. Multi-Channel (#channels)
6. How It Works (#how-it-works)
7. Industries (#industries)
8. Proven Across Industries (#case-studies) — replaced by Plan 02
9. Comparison Table (#comparison) — scaffold
10. Pricing (#pricing)
11. About (#about) — currently HERE, needs to move to position 5
12. FAQ (#faq) — scaffold
13. Booking (#booking) — scaffold
14. Footer CTA
15. Footer

Target position for About: AFTER Services (#services) and BEFORE Multi-Channel (#channels).

The About section HTML (from index.html, lines ~545–576 in original — will have shifted):
```html
  <!-- About section — repositioned in Phase 3 (TRST-06) -->
  <section class="about" id="about">
    <div class="container">
      <div class="section-header">
        <span class="section-label">About Us</span>
        <h2 class="section-title">Built by Sales People, for Sales People</h2>
      </div>
      <p class="about__text">Leadzly is a division of Elevateo Co, headquartered in Northern Ireland. ...</p>
      <div class="about__diff">
        <div class="about__diff-item fade-in">...</div>
        <div class="about__diff-item fade-in">...</div>
        <div class="about__diff-item fade-in">...</div>
      </div>
    </div>
  </section>
```

Services section close tag (for finding insertion point):
```html
  </section>
  <!-- Next section after services -->
```

Nav anchor at risk: both the nav and footer link to `href="#about"` — this works as long as `id="about"` stays on the section opening tag.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reposition About section in index.html</name>
  <files>index.html</files>
  <read_first>
    - `index.html` — read the section surrounding the current About section (search for `id="about"` to find its current position, then read ~5 lines before the opening `<section` tag and ~5 lines after the closing `</section>` tag to see its current neighbours)
    - `index.html` — read the Services section closing tag and the Multi-Channel section opening tag (search for `id="services"` and `id="channels"`) to confirm the exact insertion point
    - Confirm `id="about"` is present on the section opening tag BEFORE cutting
  </read_first>
  <action>
Perform a cut-and-paste of the About section to move it earlier in the page. This is a pure HTML repositioning — no content changes.

**Step 1 — Find and copy the entire About block**

Locate the full About section block. It starts with the comment line:
```
  <!-- About section — repositioned in Phase 3 (TRST-06) -->
```
And ends with the `</section>` that closes `<section class="about" id="about">`.

Copy the entire block including the comment line and both `<section ...>` and `</section>` tags.

**Step 2 — Delete the About block from its current position**

Remove the entire block (comment through `</section>`) from its current location (after pricing, before FAQ). After deletion, the sections around the gap should flow directly from the pricing section to the FAQ section with no gap or orphan whitespace block.

**Step 3 — Insert the About block at the new position**

The target insertion point is AFTER the Services section close tag and BEFORE the Multi-Channel (channels) section open tag.

The insertion landmark is:
- Find `<section class="channels" id="channels">` — this is the Multi-Channel section
- Insert the About block (with its leading blank line and comment) immediately BEFORE this line

After the edit, the local section order around the insertion point must read:
```
  </section>

  <!-- About section — repositioned in Phase 3 (TRST-06) -->
  <section class="about" id="about">
    ...
  </section>

  <!-- ============ CHANNELS ============ -->
  <section class="channels" id="channels">
```

**Critical pitfall guard:** Before saving, verify the About section opening tag still reads `<section class="about" id="about">` — the `id="about"` attribute must be present. If it was accidentally omitted during cut-paste, add it back. This is what nav and footer links use.
  </action>
  <verify>
    <automated>grep -n "id=\"about\"\|id=\"services\"\|id=\"channels\"\|id=\"case-studies\"" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id=\"about\"" index.html` returns 1 (not 0, not 2 — exactly one)
    - The line number of `id="about"` is GREATER than the line number of `id="services"` and LESS than the line number of `id="channels"` (verify by comparing the line numbers from the grep output above)
    - `grep "class=\"about\"" index.html` returns 1 match (section not duplicated)
    - `grep "NI-Based Team" index.html` returns 1 match (content unchanged)
    - `grep "Built by Sales People" index.html` returns 1 match (heading unchanged)
    - `grep "id=\"channels\"" index.html` returns 1 match (channels section still intact)
    - `grep "id=\"case-studies\"" index.html` returns 1 match (proven section still intact with correct id)
  </acceptance_criteria>
  <done>The About section exists once in index.html. Its line number is after Services and before Multi-Channel. id="about" is intact. All other section IDs are unaffected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verification of all Phase 3 changes</name>
  <what-built>
Phase 3 complete — all three HTML/CSS changes have been made:
1. Trust bar with 5 greyscale logos, stat badge, and guarantee badge (Plan 01)
2. "Proven Across Industries" section with 4 industry cards replacing the case-studies scaffold, testimonials section deleted (Plan 02)
3. About section repositioned between Services and Multi-Channel (this plan)
  </what-built>
  <how-to-verify>
Open index.html in a browser and perform this scroll-through:

1. **Trust bar** — immediately below the hero dark section, a slim light-grey band shows 5 greyscale logos (Elevateo, Rosey Co, Boltloop, Flooda, Bluepillar), a "100+ meetings/month" badge, and a "30-Day Guarantee" badge. Hovering a logo reveals its colour.

2. **Pain Points** — scrolling down, the green-tinted "Does This Sound Familiar?" section appears immediately after the trust bar. The trust bar is NOT inside the hero.

3. **Services** — white section with service cards appears.

4. **About section in new position** — immediately after Services, a white "Built by Sales People, for Sales People" section with 3 differentiator cards (NI-Based Team, Performance-Driven, Multi-Industry) appears BEFORE the Multi-Channel section.

5. **Multi-Channel** — dark navy section with 4 channel cards appears after About.

6. **Proven Across Industries** — scrolling past How It Works and Industries, a white section with eyebrow "Results", heading "Proven Across Industries", and subtitle "100+ meetings booked monthly across the Elevateo portfolio" appears. Four cards: Digital Marketing, AI & Automation, SaaS & Software, E-Commerce — each with a green chip tag, bold outcome, and muted detail line.

7. **No testimonials section** — scroll the full page. There must be NO section titled "What Our Clients Say" or containing "Testimonial cards coming in Phase 3".

8. **Nav anchor test** — click "#about" or "About" in the nav. The page should scroll to the repositioned About section (not a blank area).

If all 8 checks pass, type "approved". If any check fails, describe what you see.
  </how-to-verify>
  <files>index.html</files>
  <action>Human visual verification — no automated action. See how-to-verify for step-by-step browser checks.</action>
  <verify><automated>Human browser check — see how-to-verify above</automated></verify>
  <done>User types "approved" confirming all 8 visual checks passed.</done>
  <resume-signal>Type "approved" to confirm all checks passed, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 3 is verified by the human checkpoint above. All 4 active TRST requirements are addressed:
- TRST-03: 4 industry cards with tag, outcome, detail (Proven section)
- TRST-04: Industry tag chips with green styling (Proven section cards)
- TRST-05: Trust bar below hero with logos and badges (Plan 01)
- TRST-06: About section repositioned between Services and Multi-Channel
</verification>

<success_criteria>
- `grep -c "id=\"about\"" index.html` returns 1
- About section line number is between Services and Multi-Channel line numbers
- Human checkpoint approved
- No `.testimonials` section in HTML
- `grep -c "class=\"proven\"" index.html` returns 1 with id="case-studies"
</success_criteria>

<output>
After completion, create `.planning/phases/03-trust-social-proof/03-03-SUMMARY.md` following the summary template.
</output>

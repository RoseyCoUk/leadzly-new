---
phase: 04-conversion-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - page.css
autonomous: true
requirements:
  - DSGN-01

must_haves:
  truths:
    - "Hero headline renders at a minimum of 3.5rem at any viewport width"
    - "Every section eyebrow label is visually bolder (weight 700) than before"
  artifacts:
    - path: "page.css"
      provides: "Updated .hero__headline font-size clamp and .section-label font-weight"
      contains: "clamp(3.5rem"
  key_links:
    - from: "page.css .hero__headline"
      to: "index.html h1.hero__headline"
      via: "class selector"
      pattern: "clamp\\(3\\.5rem"
    - from: "page.css .section-label"
      to: "index.html span.section-label (all sections)"
      via: "class selector"
      pattern: "font-weight: 700"
---

<objective>
Apply two targeted CSS value changes that satisfy DSGN-01: raise the hero h1 minimum clamp size from 2.4rem to 3.5rem, and update the .section-label font-weight from 600 to 700.

Purpose: Ensures the hero headline is visually commanding at all viewport sizes and that eyebrow labels read as bold (700) across every section on the page — a consistency fix required before the new sections added in Plans 02–04.
Output: Two edited lines in page.css. No HTML changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-conversion-sections/04-UI-SPEC.md
@.planning/phases/04-conversion-sections/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hero h1 minimum font-size clamp</name>
  <read_first>
    - page.css (lines 270–285 — the .hero__headline rule to be edited)
  </read_first>
  <files>page.css</files>
  <action>
    Find the .hero__headline rule in page.css. The current line reads:
      font-size: clamp(2.4rem, 3.5vw + 0.5rem, 4.25rem);

    Replace it with:
      font-size: clamp(3.5rem, 3.5vw + 0.5rem, 4.75rem);

    Do NOT change font-weight (already 800 — satisfies DSGN-01 "bold at >=3.5rem").
    Do NOT modify any other property in .hero__headline.
    This is a single-value change: minimum clamp 2.4rem → 3.5rem, maximum 4.25rem → 4.75rem.
  </action>
  <verify>
    <automated>grep -n "clamp(3.5rem" '/c/Work File/Elevateo Work/Leadzly - Company/page.css'</automated>
  </verify>
  <acceptance_criteria>
    - grep "clamp(3.5rem" page.css returns exactly one match on the .hero__headline font-size line
    - grep "clamp(2.4rem" page.css returns zero matches (old value removed)
    - The .hero__headline rule still contains font-weight: 800 (unchanged)
    - No other lines in page.css were modified
  </acceptance_criteria>
  <done>page.css .hero__headline font-size uses clamp(3.5rem, 3.5vw + 0.5rem, 4.75rem)</done>
</task>

<task type="auto">
  <name>Task 2: Update .section-label font-weight from 600 to 700</name>
  <read_first>
    - page.css (lines 15–24 — the .section-label rule to be edited)
  </read_first>
  <files>page.css</files>
  <action>
    Find the .section-label rule in page.css (near line 15–24). The current rule reads:
      .section-label {
        display: inline-block;
        font-family: var(--font-body);
        font-size: var(--text-xs);
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: var(--color-primary);
        margin-bottom: var(--space-3);
      }

    Change only font-weight: 600 to font-weight: 700.

    Confirm this is the ONLY .section-label rule in page.css — there should be no overrides in style.css or base.css. Per RESEARCH.md, a single edit here is sufficient.
  </action>
  <verify>
    <automated>grep -n "font-weight: 700" '/c/Work File/Elevateo Work/Leadzly - Company/page.css' | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "font-weight: 700" page.css includes a match in the .section-label rule
    - grep "font-weight: 600" page.css does NOT match on the .section-label rule (the 600 value is removed from that rule)
    - All other font-weight declarations in page.css are unchanged
    - Only the font-weight property inside .section-label was touched — no other properties modified
  </acceptance_criteria>
  <done>.section-label rule in page.css has font-weight: 700</done>
</task>

</tasks>

<verification>
Run both automated greps before marking complete:
1. grep -n "clamp(3.5rem" page.css — must return exactly one result
2. grep -n "font-weight: 600" page.css — must NOT match the .section-label rule
3. grep -n "font-weight: 700" page.css — must include the .section-label match
</verification>

<success_criteria>
- page.css .hero__headline uses font-size: clamp(3.5rem, 3.5vw + 0.5rem, 4.75rem)
- page.css .section-label uses font-weight: 700
- No other page.css rules modified
- DSGN-01 typography requirements are met (hero bold at >=3.5rem; eyebrow labels at weight 700)
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-sections/04-01-SUMMARY.md` following the summary template.
</output>

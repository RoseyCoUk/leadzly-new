---
phase: 04-conversion-sections
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - index.html
  - page.css
autonomous: false
requirements:
  - SECT-03
  - DSGN-01

must_haves:
  truths:
    - "An ROI calculator section is visible between the FAQ and Booking sections"
    - "The three input fields (deal value, current meetings, target meetings) accept number input"
    - "Changing any input immediately updates the two output values without a page reload"
    - "With default values (deal=5000, current=4, target=20), outputs show £8,000/month and £96,000/year"
    - "Empty or zero inputs produce £0 output rather than NaN or an error"
    - "Every section on the page has a visible green eyebrow label — confirmed by human visual scan"
    - "The hero headline is visually large and bold at all viewport widths"
  artifacts:
    - path: "index.html"
      provides: "ROI calculator section HTML between .faq and .booking, plus ROI IIFE script block"
      contains: "roi__card"
    - path: "page.css"
      provides: "CSS for .roi, .roi__card, .roi__inputs, .roi__field, .roi__input, .roi__outputs, .roi__output"
      contains: "roi__output"
  key_links:
    - from: "index.html #roi-deal-value, #roi-current-meetings, #roi-target-meetings"
      to: "index.html #roi-monthly, #roi-annual (output spans)"
      via: "JS IIFE addEventListener('input', calculate)"
      pattern: "roi-monthly"
    - from: "index.html section.roi"
      to: "section.faq (above) and section.booking (below)"
      via: "DOM order — must be inserted immediately before section.booking"
      pattern: "class=\"roi\""

user_setup: []
---

<objective>
Insert the ROI calculator as a new HTML section between .faq and .booking, add all CSS for the calculator card layout, and add a vanilla JS IIFE that computes real-time pipeline projections. Then run a final human visual verification pass over all Phase 4 changes.

Purpose: Gives prospects a personalised financial case for booking — seeing their own numbers makes the value of Leadzly concrete, not abstract.
Output: Functional ROI calculator (3 inputs, 2 live outputs) + human checkpoint confirming all 4 Phase 4 requirements visually.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-conversion-sections/04-UI-SPEC.md
@.planning/phases/04-conversion-sections/04-RESEARCH.md

<interfaces>
<!-- Target insertion point in index.html: immediately BEFORE the .booking section -->
<!-- Find: <section class="booking" id="booking"> -->
<!-- Insert the new .roi section ABOVE that line -->

<!-- Exact formula (per UI-SPEC and RESEARCH.md): -->
<!--
  additionalMeetings = Math.max(0, target - current)
  CONVERSION_RATE = 0.10
  monthly = additionalMeetings * dealValue * CONVERSION_RATE
  annual  = monthly * 12
  Display: '£' + value.toLocaleString('en-GB')
-->

<!-- Default input values that produce initial display: -->
<!--
  roi-deal-value:       5000
  roi-current-meetings: 4
  roi-target-meetings:  20
  Expected initial outputs:
    monthly = (20-4) * 5000 * 0.10 = £8,000
    annual  = £8,000 * 12         = £96,000
-->

<!-- Design tokens for ROI section: -->
<!--
  Section bg:          var(--section-tint)   (#f8fafb)
  Card bg:             var(--color-bg)        (#ffffff)
  Card border:         var(--color-border)
  Card border-radius:  var(--radius-xl)
  Card shadow:         var(--shadow-md)
  Card padding:        var(--space-8)
  Output block bg:     var(--color-primary-highlight)
  Output value color:  var(--color-primary)
  Output label color:  var(--color-primary)
  Input border-radius: var(--radius-md)
-->

<!-- Section order after this plan: FAQ → ROI Calculator → Booking -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert ROI calculator HTML section and JS IIFE</name>
  <read_first>
    - index.html (lines 615–630 — the .faq closing tag and .booking opening; find insertion point)
    - .planning/phases/04-conversion-sections/04-UI-SPEC.md (§ROI Calculator — full HTML structure verbatim)
    - .planning/phases/04-conversion-sections/04-RESEARCH.md (§SECT-03 — exact calculation formula and JS IIFE code)
  </read_first>
  <files>index.html</files>
  <action>
    EDIT 1 — Insert ROI section HTML:
    Find the line:
      <section class="booking" id="booking">
    (Look for the comment "<!-- ============ BOOKING" above it as a landmark.)

    Insert the following HTML IMMEDIATELY BEFORE that line (between </section> of .faq and <section class="booking">):

    ```html
    <!-- ============ ROI CALCULATOR (SECT-03) ============ -->
    <section class="roi" id="roi">
      <div class="container">
        <div class="section-header">
          <span class="section-label">ROI Calculator</span>
          <h2 class="section-title">What Could Leadzly Be Worth to You?</h2>
          <p class="section-subtitle">Adjust the numbers below to see your projected pipeline uplift.</p>
        </div>
        <div class="roi__card">
          <div class="roi__inputs">
            <div class="roi__field">
              <label class="roi__label" for="roi-deal-value">Average deal value (&pound;)</label>
              <input class="roi__input" type="number" id="roi-deal-value" min="0" step="500" value="5000" aria-describedby="roi-deal-value-hint">
              <span class="roi__hint" id="roi-deal-value-hint">The average value of a closed deal in your pipeline</span>
            </div>
            <div class="roi__field">
              <label class="roi__label" for="roi-current-meetings">Current qualified meetings/month</label>
              <input class="roi__input" type="number" id="roi-current-meetings" min="0" step="1" value="4" aria-describedby="roi-current-meetings-hint">
              <span class="roi__hint" id="roi-current-meetings-hint">How many qualified sales meetings you&rsquo;re booking today</span>
            </div>
            <div class="roi__field">
              <label class="roi__label" for="roi-target-meetings">Target meetings/month with Leadzly</label>
              <input class="roi__input" type="number" id="roi-target-meetings" min="0" step="1" value="20" aria-describedby="roi-target-meetings-hint">
              <span class="roi__hint" id="roi-target-meetings-hint">Realistic target &mdash; our Growth plan averages 20+ meetings/month</span>
            </div>
          </div>
          <div class="roi__outputs">
            <div class="roi__output">
              <span class="roi__output-label">Additional monthly pipeline</span>
              <span class="roi__output-value" id="roi-monthly">&pound;8,000</span>
              <span class="roi__output-sub">Based on additional meetings &times; deal value &times; 10% close rate assumption</span>
            </div>
            <div class="roi__output">
              <span class="roi__output-label">Projected annual pipeline uplift</span>
              <span class="roi__output-value" id="roi-annual">&pound;96,000</span>
              <span class="roi__output-sub">Monthly uplift &times; 12</span>
            </div>
          </div>
          <p class="roi__disclaimer">Estimates assume a 10% meeting-to-close conversion rate. Actual results vary. This calculator is for illustration only.</p>
        </div>
      </div>
    </section>
    ```

    EDIT 2 — Add ROI calculator JS IIFE:
    In index.html, add a THIRD standalone <script> block immediately before </body>, after the FAQ accordion script block:

    ```html
    <script>
    // ROI Calculator
    (function() {
      var dealInput = document.getElementById('roi-deal-value');
      var currentInput = document.getElementById('roi-current-meetings');
      var targetInput = document.getElementById('roi-target-meetings');
      var monthlyOutput = document.getElementById('roi-monthly');
      var annualOutput = document.getElementById('roi-annual');

      var CONVERSION_RATE = 0.10;

      function calculate() {
        var deal = parseFloat(dealInput.value) || 0;
        var current = parseFloat(currentInput.value) || 0;
        var target = parseFloat(targetInput.value) || 0;
        var additional = Math.max(0, target - current);
        var monthly = additional * deal * CONVERSION_RATE;
        var annual = monthly * 12;
        monthlyOutput.textContent = '£' + monthly.toLocaleString('en-GB');
        annualOutput.textContent = '£' + annual.toLocaleString('en-GB');
      }

      if (dealInput && currentInput && targetInput) {
        [dealInput, currentInput, targetInput].forEach(function(el) {
          el.addEventListener('input', calculate);
        });
        calculate(); // initialise on load
      }
    })();
    </script>
    ```

    NOTE on initial HTML output values: The HTML shows £8,000 and £96,000 as static fallback text. The JS calculate() call on load will immediately overwrite these with the computed values from the default inputs. This is intentional — the static values ensure the calculator looks correct even if JS is disabled.

    PITFALL to avoid: Do NOT use the string '£' directly in JS — use '£' or '£'.toLocaleString won't break but concatenating safely is cleaner. The template above uses '£'.
  </action>
  <verify>
    <automated>grep -n "roi__card" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'</automated>
  </verify>
  <acceptance_criteria>
    - grep "roi__card" index.html returns exactly one match
    - grep "roi-deal-value" index.html returns at least 2 matches (input id + label for + hint id)
    - grep "roi-monthly" index.html returns at least 2 matches (span id + JS getElementById)
    - grep "roi-annual" index.html returns at least 2 matches (span id + JS getElementById)
    - grep "CONVERSION_RATE = 0.10" index.html returns one match (inside ROI IIFE)
    - grep "toLocaleString" index.html returns at least one match (in ROI IIFE)
    - grep "section class=\"roi\"" index.html returns one match positioned between .faq and .booking sections
    - grep "FAQ Accordion" index.html returns one match AND grep "ROI Calculator" index.html returns one match (both IIFEs present, separate)
    - The .booking section still exists and is positioned AFTER the new .roi section
  </acceptance_criteria>
  <done>index.html has .roi section between .faq and .booking; ROI JS IIFE with correct formula present as separate script block</done>
</task>

<task type="auto">
  <name>Task 2: Add ROI calculator CSS to page.css</name>
  <read_first>
    - page.css (end of file — new ROI rules go at the end, after all existing rules)
    - .planning/phases/04-conversion-sections/04-UI-SPEC.md (§ROI Calculator — Visual rules table with all token values)
  </read_first>
  <files>page.css</files>
  <action>
    Append the following CSS block to the END of page.css (after all existing rules):

    ```css
    /* ============ ROI CALCULATOR ============ */
    .roi {
      padding: clamp(var(--space-12), 8vw, var(--space-24)) 0;
      background: var(--section-tint);
    }
    .roi__card {
      background: var(--color-bg);
      border: 1px solid var(--color-border);
      border-radius: var(--radius-xl);
      padding: var(--space-8);
      box-shadow: var(--shadow-md);
      max-width: 800px;
      margin-inline: auto;
    }
    .roi__inputs {
      display: grid;
      grid-template-columns: 1fr;
      gap: var(--space-6);
    }
    @media (min-width: 768px) {
      .roi__inputs {
        grid-template-columns: repeat(3, 1fr);
      }
    }
    .roi__field {
      display: flex;
      flex-direction: column;
    }
    .roi__label {
      display: block;
      font-family: var(--font-body);
      font-size: var(--text-sm);
      font-weight: 700;
      color: var(--color-text);
      margin-bottom: var(--space-2);
    }
    .roi__input {
      width: 100%;
      padding: var(--space-3) var(--space-4);
      border: 1px solid var(--color-border);
      border-radius: var(--radius-md);
      font-family: var(--font-body);
      font-size: var(--text-sm);
      color: var(--color-text);
      background: var(--color-bg);
      box-sizing: border-box;
    }
    .roi__input:focus {
      outline: 2px solid var(--color-primary);
      outline-offset: 2px;
    }
    .roi__hint {
      display: block;
      font-family: var(--font-body);
      font-size: var(--text-xs);
      font-weight: 400;
      color: var(--color-text-faint);
      margin-top: var(--space-2);
    }
    .roi__outputs {
      display: grid;
      grid-template-columns: 1fr;
      gap: var(--space-6);
      margin-top: var(--space-8);
    }
    @media (min-width: 640px) {
      .roi__outputs {
        grid-template-columns: repeat(2, 1fr);
      }
    }
    .roi__output {
      background: var(--color-primary-highlight);
      border: 1px solid rgba(22, 163, 74, 0.2);
      border-radius: var(--radius-lg);
      padding: var(--space-6);
      text-align: center;
    }
    .roi__output-label {
      display: block;
      font-family: var(--font-body);
      font-size: var(--text-xs);
      font-weight: 700;
      color: var(--color-primary);
      text-transform: uppercase;
      letter-spacing: 0.06em;
      margin-bottom: var(--space-3);
    }
    .roi__output-value {
      display: block;
      font-family: var(--font-display);
      font-size: var(--text-xl);
      font-weight: 700;
      color: var(--color-primary);
    }
    .roi__output-sub {
      display: block;
      font-family: var(--font-body);
      font-size: var(--text-xs);
      font-weight: 400;
      color: var(--color-text-muted);
      margin-top: var(--space-2);
    }
    .roi__disclaimer {
      font-family: var(--font-body);
      font-size: var(--text-xs);
      font-weight: 400;
      color: var(--color-text-faint);
      text-align: center;
      margin-top: var(--space-6);
      font-style: italic;
    }
    ```

    Do not use any hardcoded hex colours — all colours MUST use var(--token) or rgba() as shown.
    Do not modify any existing rules — only append.
  </action>
  <verify>
    <automated>grep -n "roi__output" '/c/Work File/Elevateo Work/Leadzly - Company/page.css'</automated>
  </verify>
  <acceptance_criteria>
    - grep "roi__output" page.css returns at least 4 matches (.roi__output, .roi__output-label, .roi__output-value, .roi__output-sub)
    - grep "roi__card" page.css returns at least one match with var(--radius-xl)
    - grep "roi__inputs" page.css returns at least one match with grid-template-columns
    - grep "repeat(3, 1fr)" page.css returns at least one match (for the 3-column input layout at >=768px)
    - grep "repeat(2, 1fr)" page.css returns at least one match (for the 2-column output layout at >=640px)
    - grep "var(--section-tint)" page.css includes a .roi match
    - No hardcoded hex colour strings appear in any new .roi* CSS rule
  </acceptance_criteria>
  <done>page.css has all .roi* CSS classes; ROI section renders on var(--section-tint) background; card on white with shadow; outputs on green-tinted blocks</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Human visual verification of all Phase 4 changes</name>
  <what-built>
    Plan 01 — DSGN-01: Hero h1 minimum clamp raised to 3.5rem; .section-label font-weight raised to 700
    Plan 02 — SECT-01: Comparison table on dark navy background with 8 rows; Leadzly column green-highlighted
    Plan 03 — SECT-02: FAQ accordion with 7 questions; one-at-a-time toggle; chevron rotation
    Plan 04 — SECT-03: ROI calculator with 3 inputs; real-time output update; £8,000/£96,000 at defaults
  </what-built>
  <how-to-verify>
    Open index.html in a browser (drag-and-drop or use Live Server). Then check each item:

    1. DSGN-01 — Hero headline:
       - At desktop (1200px+), the hero h1 "Fill Your Calendar" appears visually large and commanding
       - In DevTools → inspect h1 → computed font-size should be at or above 56px
       - All section eyebrow labels (e.g. "Sound Familiar?", "What We Do", "Our Approach") appear in bold green small caps

    2. SECT-01 — Comparison table:
       - Scroll to the "How We Stack Up" section — it should have a DARK NAVY background
       - The "Leadzly" column header has a green-tinted background and green text — visually distinct
       - Count 8 rows in the table body
       - At 375px viewport (DevTools mobile), the table scrolls horizontally inside its container

    3. SECT-02 — FAQ accordion:
       - Scroll to "Common Questions, Honest Answers" section
       - Click the first question — it should expand smoothly; the chevron rotates 180°
       - Click a second question — the first should collapse, the second expand
       - Click the open question again — it should collapse (toggle off)
       - Tab to a button using keyboard, press Enter — it should open/close

    4. SECT-03 — ROI calculator:
       - Scroll to "What Could Leadzly Be Worth to You?" section
       - Default outputs should show £8,000 (monthly) and £96,000 (annual)
       - Change deal value to 10000 — outputs should update instantly to £16,000 and £192,000
       - Clear the deal value field — outputs should show £0 (not NaN or blank)
       - Verify section sits between FAQ and Booking sections in the page order
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 checks pass. Or describe any issues found (e.g. "comparison table background is still white", "FAQ question 3 not collapsing") and the executor will fix before marking complete.</resume-signal>
</task>

</tasks>

<verification>
Automated greps before human checkpoint:
1. grep "roi__card" index.html — must return one match
2. grep "CONVERSION_RATE = 0.10" index.html — must return one match
3. grep "roi__output" page.css — must return 4+ matches
4. grep "repeat(3, 1fr)" page.css — must return one match
5. grep "repeat(2, 1fr)" page.css — must return one match
6. grep "section class=\"roi\"" index.html — must appear between .faq and .booking
</verification>

<success_criteria>
- ROI section is visually present between FAQ and Booking
- Default inputs produce £8,000 monthly and £96,000 annual output values
- Changing any input updates outputs in real time without page reload
- Empty inputs produce £0 not NaN
- Human checkpoint approved (all 4 checks: hero, comparison, FAQ, ROI)
- SECT-03 and DSGN-01 satisfied
- Phase 4 complete: SECT-01, SECT-02, SECT-03, DSGN-01 all done
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-sections/04-04-SUMMARY.md` following the summary template.
</output>

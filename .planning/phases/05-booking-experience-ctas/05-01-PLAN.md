---
phase: 05-booking-experience-ctas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - page.css
autonomous: true
requirements:
  - BOOK-01

must_haves:
  truths:
    - "Visitor can see a two-column booking layout with call benefits on the left and a live Calendly calendar on the right"
    - "The Calendly widget loads and renders a month-view calendar without internal scrollbars"
    - "Calendly script and CSS are loaded exactly once from the CDN"
  artifacts:
    - path: "index.html"
      provides: "Booking section two-column grid HTML replacing scaffold placeholder"
      contains: "booking__grid"
    - path: "index.html"
      provides: "Calendly CDN script and CSS link in <head>"
      contains: "assets.calendly.com/assets/external/widget.js"
    - path: "page.css"
      provides: "Booking grid layout and benefit list styles"
      contains: ".booking__grid"
  key_links:
    - from: "index.html .calendly-inline-widget"
      to: "Calendly CDN widget.js"
      via: "data-url attribute + async script in <head>"
      pattern: "data-url=\"https://calendly.com/allan-chan-leadzly\""
---

<objective>
Replace the Phase 5 booking section scaffold placeholder with a real two-column layout: left column sells the call with 5 bullet benefits, right column contains the live Calendly inline embed. Load Calendly CSS and JS from CDN in `<head>`.

Purpose: Visitors can book a strategy call without leaving leadzly.co — the primary conversion action of the entire page.
Output: Live Calendly calendar embedded in booking section, two-column grid layout with benefit copy, responsive stack on mobile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-booking-experience-ctas/05-UI-SPEC.md
@.planning/phases/05-booking-experience-ctas/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Calendly CDN resources to head + replace booking scaffold with two-column HTML</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 1–30 for head section; lines 817–828 for booking scaffold to replace)
  </read_first>
  <action>
**Step A — Add Calendly CDN to `<head>`.**
Locate the `<head>` section (around line 1–30). Before the closing `</head>` tag, add these two lines (check first — if either URL already exists, skip adding that line):
```html
<link href="https://assets.calendly.com/assets/external/widget.css" rel="stylesheet">
<script src="https://assets.calendly.com/assets/external/widget.js" type="text/javascript" async></script>
```

**Step B — Replace booking scaffold placeholder.**
Find and replace this exact line (line 826):
```html
      <!-- Calendly embed added in Phase 5 (BOOK-01) -->
      <p class="scaffold-placeholder">Booking calendar coming in Phase 5.</p>
```
Replace with the full two-column grid structure:
```html
      <div class="booking__grid">
        <div class="booking__content">
          <h3 class="booking__subheading">What to expect on the call</h3>
          <ul class="booking__benefits" role="list">
            <li class="booking__benefit">
              <span class="booking__benefit-icon" aria-hidden="true">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              </span>
              <span>We'll audit your current outbound setup — no fluff</span>
            </li>
            <li class="booking__benefit">
              <span class="booking__benefit-icon" aria-hidden="true">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              </span>
              <span>You'll get a bespoke meetings forecast for your ICP</span>
            </li>
            <li class="booking__benefit">
              <span class="booking__benefit-icon" aria-hidden="true">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              </span>
              <span>We'll show you exactly how we'd run your outreach</span>
            </li>
            <li class="booking__benefit">
              <span class="booking__benefit-icon" aria-hidden="true">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              </span>
              <span>No obligation — cancel or reschedule any time</span>
            </li>
            <li class="booking__benefit">
              <span class="booking__benefit-icon" aria-hidden="true">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
              </span>
              <span>30-minute call, respects your time</span>
            </li>
          </ul>
          <p class="booking__reassurance">No hard sell. No obligation.</p>
        </div>
        <div class="booking__calendar-wrap">
          <div class="calendly-inline-widget"
               data-url="https://calendly.com/allan-chan-leadzly"
               style="min-width:320px;height:700px;"></div>
        </div>
      </div>
```
  </action>
  <verify>
    <automated>grep -c "booking__grid" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `assets.calendly.com/assets/external/widget.css` (exactly once)
    - index.html contains `assets.calendly.com/assets/external/widget.js` (exactly once)
    - index.html contains `class="booking__grid"`
    - index.html contains `data-url="https://calendly.com/allan-chan-leadzly"`
    - index.html contains `style="min-width:320px;height:700px;"`
    - index.html does NOT contain `scaffold-placeholder` inside the booking section
    - index.html contains all 5 benefit text strings: "audit your current outbound", "bespoke meetings forecast", "exactly how we'd run", "No obligation — cancel", "30-minute call"
  </acceptance_criteria>
  <done>Booking section scaffold replaced with full two-column grid; Calendly CDN loaded in head; page opens in browser and shows two-column layout with calendar loading on the right.</done>
</task>

<task type="auto">
  <name>Task 2: Add booking grid CSS and benefit list styles to page.css</name>
  <files>page.css</files>
  <read_first>
    - page.css (last 30 lines — append after .roi__disclaimer rule)
  </read_first>
  <action>
Append the following CSS block to the end of `page.css` (after the `.roi__disclaimer` rule at the bottom of the file):

```css
/* ===== BOOKING SECTION (BOOK-01) ===== */
.booking__grid {
  display: grid;
  grid-template-columns: 1fr 1.2fr;
  gap: var(--space-8);
  align-items: start;
  margin-top: var(--space-8);
}
.booking__subheading {
  font-family: var(--font-display);
  font-size: var(--text-xl);
  font-weight: 700;
  color: var(--color-heading);
  margin-bottom: var(--space-6);
}
.booking__benefits {
  list-style: none;
  padding: 0;
  margin: 0 0 var(--space-6) 0;
  display: flex;
  flex-direction: column;
  gap: var(--space-4);
}
.booking__benefit {
  display: flex;
  align-items: flex-start;
  gap: var(--space-3);
  font-family: var(--font-body);
  font-size: var(--text-base);
  color: var(--color-text);
  line-height: 1.5;
}
.booking__benefit-icon {
  flex-shrink: 0;
  color: var(--color-primary);
  margin-top: 2px;
}
.booking__reassurance {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: 400;
  color: var(--color-text-muted);
}
.booking__calendar-wrap {
  border-radius: var(--radius-xl);
  overflow: hidden;
}
@media (max-width: 767px) {
  .booking__grid {
    grid-template-columns: 1fr;
  }
  .booking__calendar-wrap .calendly-inline-widget {
    height: 650px !important;
  }
}
```
  </action>
  <verify>
    <automated>grep -c "booking__grid" '/c/Work File/Elevateo Work/Leadzly - Company/page.css'</automated>
  </verify>
  <acceptance_criteria>
    - page.css contains `.booking__grid`
    - page.css contains `grid-template-columns: 1fr 1.2fr`
    - page.css contains `.booking__benefit-icon`
    - page.css contains `.booking__calendar-wrap`
    - page.css contains `@media (max-width: 767px)` block with `grid-template-columns: 1fr`
    - page.css contains `height: 650px !important` inside mobile media query (Calendly height override)
  </acceptance_criteria>
  <done>All booking section CSS is in page.css; two-column grid renders correctly in browser; on viewport below 768px, layout stacks to single column.</done>
</task>

</tasks>

<verification>
grep "booking__grid" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'
grep "calendly-inline-widget" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'
grep "assets.calendly.com" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'
grep "booking__grid" '/c/Work File/Elevateo Work/Leadzly - Company/page.css'
</verification>

<success_criteria>
1. Booking section two-column grid renders in browser — benefits list left, Calendly calendar right
2. Calendly CDN script and CSS appear exactly once in index.html head
3. Mobile viewport (375px) stacks to single column with calendar below benefits
4. Calendly calendar height is 700px on desktop, 650px on mobile — no internal scrollbar
</success_criteria>

<output>
After completion, create `.planning/phases/05-booking-experience-ctas/05-01-SUMMARY.md`
</output>

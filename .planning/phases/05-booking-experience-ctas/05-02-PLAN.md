---
phase: 05-booking-experience-ctas
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - index.html
  - page.css
autonomous: true
requirements:
  - BOOK-02

must_haves:
  truths:
    - "A floating sticky button is invisible for the first 3 seconds after page load, then slides up into view in the bottom-right corner"
    - "The floating button has a white pulsing dot that animates continuously to signal live availability"
    - "The floating button is completely hidden on mobile viewports (< 640px)"
  artifacts:
    - path: "index.html"
      provides: "Floating CTA HTML div before </body>"
      contains: "floating-cta"
    - path: "page.css"
      provides: "Floating CTA styles — hidden state, visible modifier, pulse ring animation"
      contains: ".floating-cta"
    - path: "index.html"
      provides: "JS IIFE that adds floating-cta--visible class after 3000ms"
      contains: "floating-cta--visible"
  key_links:
    - from: "JS IIFE setTimeout 3000ms"
      to: "div#floating-cta"
      via: "classList.add('floating-cta--visible')"
      pattern: "floating-cta--visible"
    - from: ".floating-cta--visible CSS modifier"
      to: ".floating-cta base state"
      via: "opacity 1 + translateY(0) transition"
      pattern: "floating-cta--visible"
---

<objective>
Add a floating sticky CTA button fixed to the bottom-right corner of the viewport. It starts invisible (opacity 0, translated down 16px, pointer-events none) and slides into view 3 seconds after page load via a JS IIFE. A pulsing white dot signals live availability. Hidden entirely on mobile (< 640px).

Purpose: Persistent conversion surface — always visible to returning scrollers without blocking content on mobile.
Output: Floating sticky button in bottom-right, 3-second entrance delay, continuous pulse animation, hidden on mobile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-booking-experience-ctas/05-UI-SPEC.md
@.planning/phases/05-booking-experience-ctas/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add floating CTA HTML before </body> and JS IIFE script block</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 1060–1068 — end of file, after ROI calculator script, before </body>)
  </read_first>
  <action>
**Step A — Add floating CTA HTML.**
Find the closing `</body>` tag (currently at the end of index.html, after the ROI calculator script block). Insert the following HTML block immediately before `</body>`:

```html
  <!-- ===== FLOATING STICKY CTA (BOOK-02) ===== -->
  <div class="floating-cta" id="floating-cta" role="complementary" aria-label="Book a strategy call">
    <a href="https://calendly.com/allan-chan-leadzly"
       target="_blank"
       rel="noopener noreferrer"
       class="floating-cta__btn btn btn--primary">
      <span class="floating-cta__dot" aria-hidden="true"></span>
      Book a Call
    </a>
  </div>
```

**Step B — Add floating CTA JS IIFE.**
After the floating CTA HTML (still before `</body>`), add a new `<script>` block:

```html
  <script>
  // Floating CTA — animate in after 3 seconds
  (function() {
    setTimeout(function() {
      var btn = document.getElementById('floating-cta');
      if (btn) btn.classList.add('floating-cta--visible');
    }, 3000);
  })();
  </script>
```
  </action>
  <verify>
    <automated>grep -c "floating-cta" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `id="floating-cta"`
    - index.html contains `role="complementary"`
    - index.html contains `aria-label="Book a strategy call"`
    - index.html contains `class="floating-cta__dot"`
    - index.html contains `floating-cta--visible` (in JS setTimeout block)
    - index.html contains `setTimeout` with `3000` as the delay value
    - The floating-cta div appears after `</footer>` and before `</body>`
  </acceptance_criteria>
  <done>Floating CTA HTML and IIFE script present in index.html; no syntax errors; file ends with </body></html>.</done>
</task>

<task type="auto">
  <name>Task 2: Add floating CTA CSS (base state, visible modifier, pulse ring, mobile hide) to page.css</name>
  <files>page.css</files>
  <read_first>
    - page.css (last 10 lines — confirm end of file after booking CSS added in plan 01)
  </read_first>
  <action>
Append the following CSS block to the end of `page.css` (after the booking section CSS added in plan 01):

```css
/* ===== FLOATING STICKY CTA (BOOK-02) ===== */
.floating-cta {
  position: fixed;
  bottom: 24px;
  right: 24px;
  z-index: 9999;
  opacity: 0;
  transform: translateY(16px);
  transition: opacity 0.4s ease, transform 0.4s ease;
  pointer-events: none;
}
.floating-cta--visible {
  opacity: 1;
  transform: translateY(0);
  pointer-events: auto;
}
.floating-cta__btn {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  padding: var(--space-4) var(--space-6);
  white-space: nowrap;
}
.floating-cta__dot {
  display: inline-block;
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #ffffff;
  position: relative;
  flex-shrink: 0;
}
.floating-cta__dot::before {
  content: '';
  position: absolute;
  inset: -4px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.4);
  animation: pulse-ring 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
}
@keyframes pulse-ring {
  0%, 100% { transform: scale(1); opacity: 0.6; }
  50%       { transform: scale(1.5); opacity: 0; }
}
@media (max-width: 639px) {
  .floating-cta { display: none; }
}
```
  </action>
  <verify>
    <automated>grep -c "floating-cta" '/c/Work File/Elevateo Work/Leadzly - Company/page.css'</automated>
  </verify>
  <acceptance_criteria>
    - page.css contains `.floating-cta` with `position: fixed`
    - page.css contains `bottom: 24px` and `right: 24px`
    - page.css contains `z-index: 9999`
    - page.css contains `pointer-events: none` on base state
    - page.css contains `.floating-cta--visible` with `opacity: 1`
    - page.css contains `.floating-cta__dot::before` with `animation: pulse-ring`
    - page.css contains `@keyframes pulse-ring`
    - page.css contains `@media (max-width: 639px)` block with `.floating-cta { display: none; }`
  </acceptance_criteria>
  <done>Floating CTA CSS in page.css; button invisible on load; adds floating-cta--visible class after 3s in browser; pulse dot animation plays continuously; button is not visible at 375px viewport width.</done>
</task>

</tasks>

<verification>
grep "floating-cta" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'
grep "floating-cta" '/c/Work File/Elevateo Work/Leadzly - Company/page.css'
grep "pulse-ring" '/c/Work File/Elevateo Work/Leadzly - Company/page.css'
</verification>

<success_criteria>
1. Page loads → bottom-right corner is empty for 3 seconds → "Book a Call" button slides up into view
2. Pulsing white dot is visible on the button at all times after appearance
3. Resizing to 375px viewport: floating button is not visible
4. Button links to https://calendly.com/allan-chan-leadzly with target="_blank" rel="noopener noreferrer"
</success_criteria>

<output>
After completion, create `.planning/phases/05-booking-experience-ctas/05-02-SUMMARY.md`
</output>

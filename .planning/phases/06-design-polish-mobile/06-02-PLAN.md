---
phase: 06-design-polish-mobile
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - index.html
autonomous: false
requirements:
  - DSGN-03

must_haves:
  truths:
    - "Each section below the hero slides up into view as the user scrolls down"
    - "Hero and trust-bar sections load fully visible with no fade effect"
    - "All card elements within sections continue to stagger-animate as before"
    - "The page feels intentional and polished at any scroll position"
  artifacts:
    - path: "index.html"
      provides: "fade-in class on all below-fold section elements"
      contains: "class=\"pain-points fade-in\""
  key_links:
    - from: "index.html <section class='... fade-in'>"
      to: "page.css .fade-in--hidden / .fade-in--visible"
      via: "JS IntersectionObserver (index.html ~line 982)"
      pattern: "fade-in"
    - from: "JS observer guard"
      to: "hero and trust-bar sections"
      via: "rect.top > window.innerHeight * 0.85 check"
      pattern: "window\\.innerHeight.*0\\.85"
---

<objective>
Add the `fade-in` class to all below-fold section elements in index.html so the upgraded CSS + JS observer from Plan 01 applies section-level scroll animations. Then run a human-verify checkpoint to confirm the full Phase 6 polish across all viewports.

Purpose: Sections fade and slide up as the user scrolls — the page feels alive and intentional, not static.
Output: Updated index.html with fade-in on 13 sections. Human sign-off on full Phase 6 visual quality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-design-polish-mobile/06-RESEARCH.md
@.planning/phases/06-design-polish-mobile/06-01-SUMMARY.md

<interfaces>
<!-- Current section tags in index.html (from grep audit) -->
line 150:  <section class="hero" id="hero">
line 219:  <section class="trust-bar" aria-label="Social proof">
line 240:  <section class="pain-points" id="pain-points">
line 295:  <section class="services" id="services">
line 335:  <section class="about" id="about">
line 369:  <section class="channels" id="channels">
line 410:  <section class="how" id="how-it-works">
line 445:  <section class="industries" id="industries">
line 506:  <section class="proven" id="case-studies">
line 539:  <section class="comparison" id="comparison">
line 637:  <section class="pricing" id="pricing">
line 693:  <section class="faq" id="faq">
line 777:  <section class="roi" id="roi">
line 823:  <section class="booking" id="booking">
line 877:  <section class="cta-banner" id="cta">

<!-- JS observer guard (index.html ~line 986-991) -->
els.forEach(function(el) {
  var rect = el.getBoundingClientRect();
  if (rect.top > window.innerHeight * 0.85) {
    el.classList.add('fade-in--hidden');
  }
});
// Guard means hero and trust-bar (above fold at load) will NOT be set to hidden — safe to add fade-in to all sections.

<!-- Upgraded fade-in CSS from Plan 01 (now in page.css) -->
.fade-in--hidden { opacity: 0; transform: translateY(20px); }
.fade-in--visible { opacity: 1; transform: translateY(0); }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add fade-in class to below-fold sections in index.html</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 150–880 — all section opening tags to see exact current class attributes before editing)
    - index.html (lines 980–1010 — JS observer IIFE to confirm guard logic is unchanged)
  </read_first>
  <action>
Add `fade-in` to the `class` attribute of the following section elements. Do NOT add it to `<section class="hero">` or `<section class="trust-bar">` — those are above the fold and the JS guard handles them, but adding is harmless; the real reason to skip them is that they're always visible and adding fade-in adds noise with no visual gain.

Apply to these 13 sections by editing each opening `<section>` tag:

| Current tag | Updated tag |
|-------------|-------------|
| `<section class="pain-points" id="pain-points">` | `<section class="pain-points fade-in" id="pain-points">` |
| `<section class="services" id="services">` | `<section class="services fade-in" id="services">` |
| `<section class="about" id="about">` | `<section class="about fade-in" id="about">` |
| `<section class="channels" id="channels">` | `<section class="channels fade-in" id="channels">` |
| `<section class="how" id="how-it-works">` | `<section class="how fade-in" id="how-it-works">` |
| `<section class="industries" id="industries">` | `<section class="industries fade-in" id="industries">` |
| `<section class="proven" id="case-studies">` | `<section class="proven fade-in" id="case-studies">` |
| `<section class="comparison" id="comparison">` | `<section class="comparison fade-in" id="comparison">` |
| `<section class="pricing" id="pricing">` | `<section class="pricing fade-in" id="pricing">` |
| `<section class="faq" id="faq">` | `<section class="faq fade-in" id="faq">` |
| `<section class="roi" id="roi">` | `<section class="roi fade-in" id="roi">` |
| `<section class="booking" id="booking">` | `<section class="booking fade-in" id="booking">` |
| `<section class="cta-banner" id="cta">` | `<section class="cta-banner fade-in" id="cta">` |

IMPORTANT — stagger behaviour: The JS observer calculates stagger delay using `parent.querySelectorAll('.fade-in')` to find sibling index. When a `<section>` itself is `.fade-in`, it is the observed element; the parent is `<main>` or `<body>`. Multiple sections are siblings in `<main>`, so they will stagger at 60ms each. This is acceptable — sections appear in rapid succession, not all at once.

Do NOT modify the JS observer IIFE — no changes to the script blocks.
Do NOT add fade-in to `<section class="hero">` or `<section class="trust-bar">`.
  </action>
  <verify>
    <automated>grep -c "fade-in" '/c/Work File/Elevateo Work/Leadzly - Company/index.html'</automated>
  </verify>
  <acceptance_criteria>
    - grep "pain-points fade-in" index.html returns 1 match
    - grep "services fade-in" index.html returns 1 match
    - grep "about fade-in" index.html returns 1 match
    - grep "channels fade-in" index.html returns 1 match
    - grep "how fade-in" index.html returns 1 match
    - grep "industries fade-in" index.html returns 1 match
    - grep "proven fade-in" index.html returns 1 match
    - grep "comparison fade-in" index.html returns 1 match
    - grep "pricing fade-in" index.html returns 1 match
    - grep "faq fade-in" index.html returns 1 match
    - grep "roi fade-in" index.html returns 1 match
    - grep "booking fade-in" index.html returns 1 match
    - grep "cta-banner fade-in" index.html returns 1 match
    - grep "hero fade-in" index.html returns 0 matches (hero intentionally excluded)
    - grep "trust-bar fade-in" index.html returns 0 matches (trust-bar intentionally excluded)
    - grep -c "fade-in" index.html returns a higher count than before (cards already had fade-in in earlier phases, sections now added)
  </acceptance_criteria>
  <done>13 below-fold sections have the fade-in class. The JS observer will apply fade-in--hidden to each section below the fold at load time and animate them in with translateY(20px) → translateY(0) as the user scrolls.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — Phase 6 full visual review</name>
  <what-built>
    Plan 01 added hover lift effects (translateY(-4px) + shadow-lg + green border) to all 8 card classes and upgraded the fade-in animation to include translateY. Plan 02 added the fade-in class to 13 sections so they animate on scroll.
  </what-built>
  <how-to-verify>
Open index.html in a browser and run through this checklist:

**Desktop (1440px or full window):**
1. Hover over a pain-point card — it should lift ~4px upward, show a stronger shadow, and gain a green border highlight
2. Hover over a service card — same lift effect (previously had mild shadow only)
3. Hover over a channel card (dark section) — lift effect visible
4. Hover over a process step in the How It Works section — lift effect
5. Hover over a proven industry card — lift + green border
6. Hover over the pricing cards — all three lift; the centre (featured) card should maintain its slight scale and also lift
7. Hover over the ROI calculator card — lift effect
8. Hover over the About section difference items — lift + green border appears on a previously borderless item
9. Scroll to the bottom, then refresh, then scroll from the top — sections should slide up from ~20px below as they come into view (not just fade, but move up)
10. Verify hero and trust-bar sections are fully visible on load with no fade or delay

**Mobile (375px — use browser DevTools device emulation):**
11. Set viewport to 375px width
12. Scroll the full page — confirm all grids are single column (pain-points, services, channels, proven, pricing, footer)
13. Check the How It Works section — steps should stack vertically with no connector arrows between them
14. Scroll to the comparison table — it should scroll horizontally (swipe/drag the table row)
15. Wait 3 seconds after load — confirm the floating CTA button does NOT appear on 375px (it is hidden on mobile)

**Animation quality:**
16. The section slide-up should feel smooth, not jarring — sections should appear to glide upward into position
17. No sections should flash or blink — the JS guard means above-fold sections load visible immediately
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. Or describe any issues (e.g., "pricing card scale lost on hover", "comparison table not scrolling") and the executor will fix before re-verification.</resume-signal>
</task>

</tasks>

<verification>
After Task 1:
- `grep -c "fade-in" index.html` returns more matches than baseline (cards from previous phases already had it; count increases by 13 section additions)
- `grep "hero fade-in" index.html` returns 0 (hero intentionally excluded)
- `grep "cta-banner fade-in" index.html` returns 1

After human verify checkpoint passes: Phase 6 complete.
</verification>

<success_criteria>
- 13 sections have the fade-in class in index.html
- Hero and trust-bar sections do NOT have fade-in class
- Human verify checkpoint passes at 375px, 768px, and 1440px viewports
- All hover effects confirmed working on desktop
- All mobile layout checks confirmed passing (single column grids, stacked process steps, horizontal comparison scroll, no floating CTA)
- Sections slide up smoothly on scroll with no visual flash or CLS
</success_criteria>

<output>
After completion, create `.planning/phases/06-design-polish-mobile/06-02-SUMMARY.md`
</output>

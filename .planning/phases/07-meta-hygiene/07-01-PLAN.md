---
phase: 07-meta-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
must_haves:
  truths:
    - "Google can identify https://leadzly.co/ as the canonical URL — no duplicate-URL confusion"
    - "Googlebot receives an explicit index, follow directive from the page head"
    - "Sharing leadzly.co on Twitter/X renders a summary card with title, description, and image"
  artifacts:
    - path: "index.html"
      provides: "canonical link element, robots meta tag, all 4 twitter: meta tags"
      contains: "rel=\"canonical\""
  key_links:
    - from: "index.html <head>"
      to: "https://leadzly.co/"
      via: "link rel=canonical href"
      pattern: "canonical.*leadzly\\.co"
    - from: "index.html <head>"
      to: "X/Twitter card bot"
      via: "meta name=twitter:card"
      pattern: "twitter:card.*summary"
---

<objective>
Add three groups of SEO meta tags to the index.html head: a canonical link element (META-01), a robots meta directive (META-02), and four Twitter Card meta tags (META-03).

Purpose: Search engines confirm the preferred URL, crawlers receive an explicit index/follow directive, and Twitter/X renders a branded link preview card with title, description, and logo image.
Output: index.html head contains canonical, robots, and twitter:* tags inserted between the existing OG block and the first favicon link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-meta-hygiene/07-RESEARCH.md

<interfaces>
<!-- Current head state (from index.html lines 26–37) — insertion point is between og:url and the first link rel=icon -->

Existing head sequence (relevant excerpt):
```html
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Leadzly — Scale Sales Without Hiring | B2B Sales Outsourcing</title>
<meta name="description" content="Leadzly books 20+ qualified meetings per month through dedicated telesales. B2B sales outsourcing, appointment booking, and BDM provision. Book a free strategy call.">
<meta property="og:title" content="Leadzly — Scale Sales Without Hiring">
<meta property="og:description" content="We book 20+ qualified meetings per month through dedicated telesales, so you can focus on closing deals.">
<meta property="og:type" content="website">
<meta property="og:image" content="https://leadzly.co/Leadzly-logo_with-text.png">
<meta property="og:image:type" content="image/png">
<meta property="og:image:alt" content="Leadzly logo">
<meta property="og:url" content="https://leadzly.co/">
<link rel="icon" type="image/svg+xml" href="./Leadzly-logo_without-text.svg">  <!-- INSERT BEFORE THIS LINE -->
```

Confirmed absent: no existing `rel="canonical"`, no `name="robots"`, no `twitter:` tags — safe to add.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical link and robots meta to index.html head</name>
  <files>index.html</files>
  <read_first>
    - index.html (read the full current head block — lines 1–45 — to see the exact og:url line to insert after)
    - .planning/phases/07-meta-hygiene/07-RESEARCH.md (canonical and robots code examples section)
  </read_first>
  <action>
In index.html, find the line:

```html
  <meta property="og:url" content="https://leadzly.co/">
```

Immediately after that line (before `<link rel="icon">`), insert these two lines:

```html
  <link rel="canonical" href="https://leadzly.co/">
  <meta name="robots" content="index, follow">
```

Exact attribute requirements:
- Canonical: `rel="canonical"` attribute, `href="https://leadzly.co/"` with trailing slash matching the og:url above it
- Robots: `name="robots"` attribute (NOT `property="robots"`), `content="index, follow"` with a space after the comma

Do NOT use `property=` on either tag. Do NOT omit the trailing slash from the canonical href. Do NOT add any comments — the tags are self-explanatory.
  </action>
  <verify>
    <automated>grep -n 'rel="canonical"' index.html && grep -n 'name="robots"' index.html</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains the exact string: `rel="canonical" href="https://leadzly.co/"`
    - index.html contains the exact string: `name="robots" content="index, follow"`
    - Both lines appear after the `og:url` meta tag and before the first `link rel="icon"` tag
    - No existing `rel="canonical"` or `name="robots"` duplicates exist (grep count = 1 each)
  </acceptance_criteria>
  <done>index.html head contains canonical link and robots meta tag, both with correct attribute names and values</done>
</task>

<task type="auto">
  <name>Task 2: Add Twitter Card meta tags to index.html head</name>
  <files>index.html</files>
  <read_first>
    - index.html (confirm Task 1 is in place — see the canonical and robots lines just inserted, then insert Twitter tags after them)
    - .planning/phases/07-meta-hygiene/07-RESEARCH.md (META-03 code examples section — note: name= not property=)
  </read_first>
  <action>
In index.html, find the line just inserted by Task 1:

```html
  <meta name="robots" content="index, follow">
```

Immediately after that line (still before `<link rel="icon">`), insert these four lines:

```html
  <meta name="twitter:card" content="summary">
  <meta name="twitter:title" content="Leadzly — Scale Sales Without Hiring | B2B Sales Outsourcing">
  <meta name="twitter:description" content="Leadzly books 20+ qualified meetings per month through dedicated telesales. B2B sales outsourcing, appointment booking, and BDM provision. Book a free strategy call.">
  <meta name="twitter:image" content="https://leadzly.co/Leadzly-logo_with-text.png">
```

Exact attribute and value requirements:
- ALL four tags use `name=` attribute — NOT `property=` (Twitter Card tags are NOT Open Graph)
- `twitter:card` value is `summary` (NOT `summary_large_image` — the logo is square, not 2:1 landscape)
- `twitter:title` value: `Leadzly — Scale Sales Without Hiring | B2B Sales Outsourcing` (em dash, not hyphen — match the `<title>` tag exactly)
- `twitter:description` value: `Leadzly books 20+ qualified meetings per month through dedicated telesales. B2B sales outsourcing, appointment booking, and BDM provision. Book a free strategy call.` (match the `<meta name="description">` content exactly)
- `twitter:image` value: `https://leadzly.co/Leadzly-logo_with-text.png` (absolute HTTPS URL — no relative path)

Do NOT include `twitter:site` (Leadzly's X handle is not confirmed — omit optional tags).
  </action>
  <verify>
    <automated>grep -c 'name="twitter:' index.html</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'name="twitter:' index.html outputs `4`
    - index.html contains exactly: `name="twitter:card" content="summary"`
    - index.html contains exactly: `name="twitter:title" content="Leadzly — Scale Sales Without Hiring | B2B Sales Outsourcing"`
    - index.html contains exactly: `name="twitter:image" content="https://leadzly.co/Leadzly-logo_with-text.png"`
    - No tag uses `property="twitter:` — all four use `name="twitter:`
    - All four twitter: tags appear after the robots meta and before the first `link rel="icon"` line
  </acceptance_criteria>
  <done>All 4 required Twitter Card meta tags present in index.html head with correct name= attributes, correct content values, and absolute image URL</done>
</task>

</tasks>

<verification>
Run after both tasks complete:

```bash
grep -n 'canonical\|name="robots"\|twitter:' index.html
```

Expected output: 6 matching lines — 1 canonical, 1 robots, 4 twitter: tags — all within the head block, between og:url and the first link rel="icon".

Also verify insertion order is correct (canonical before robots before twitter: block) by checking line numbers in output.
</verification>

<success_criteria>
- `grep 'rel="canonical"' index.html` returns exactly 1 match with href="https://leadzly.co/"
- `grep 'name="robots"' index.html` returns exactly 1 match with content="index, follow"
- `grep -c 'name="twitter:' index.html` returns 4
- All 6 new tags appear between og:url and the first link rel="icon" in the head block
- META-01, META-02, META-03 are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-meta-hygiene/07-01-SUMMARY.md` using the summary template.
</output>

---
phase: 08-structured-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - SCHEMA-01
must_haves:
  truths:
    - "index.html <head> contains a <script type=\"application/ld+json\"> block for Organization + LocalBusiness"
    - "The block includes name, url, logo, description, areaServed (UK + NI), contactPoint, address, and parentOrganization"
    - "The JSON is syntactically valid — no trailing commas, no unclosed brackets, no HTML entities"
    - "No contact detail (phone, email) appears in the schema that is not visible on the page"
  artifacts:
    - path: "index.html"
      provides: "Organization + LocalBusiness JSON-LD block in <head>"
      contains: "application/ld+json"
  key_links:
    - from: "index.html <head>"
      to: "schema.org/LocalBusiness"
      via: "@type: [\"LocalBusiness\", \"ProfessionalService\"]"
      pattern: "LocalBusiness"
---

<objective>
Add the Organization + LocalBusiness JSON-LD block to index.html <head>.

Purpose: Signals Leadzly's business identity, location (Northern Ireland / UK), and service type to Google's Knowledge Panel crawler. Required for SCHEMA-01.
Output: One <script type="application/ld+json"> block inserted before </head> in index.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-structured-data/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Organization + LocalBusiness JSON-LD block into index.html head</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 1–60 to see current head structure and where to insert)
    - .planning/phases/08-structured-data/08-RESEARCH.md (Pattern 1 and Anti-Patterns sections)
  </read_first>
  <action>
Insert the following JSON-LD block into index.html immediately before the closing `</head>` tag. The block must use `<script type="application/ld+json">` — no other script type.

The exact block to insert (per SCHEMA-01):

```html
  <!-- Structured Data: Organization + LocalBusiness (SCHEMA-01) -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": ["LocalBusiness", "ProfessionalService"],
    "name": "Leadzly",
    "url": "https://leadzly.co/",
    "logo": "https://leadzly.co/Leadzly-logo_with-text.png",
    "description": "B2B sales outsourcing agency based in Northern Ireland. We book 20+ qualified meetings per month through dedicated telesales, appointment booking, and BDM provision.",
    "areaServed": [
      { "@type": "AdministrativeArea", "name": "United Kingdom" },
      { "@type": "AdministrativeArea", "name": "Northern Ireland" }
    ],
    "contactPoint": {
      "@type": "ContactPoint",
      "contactType": "sales",
      "url": "https://calendly.com/allan-chan-leadzly",
      "availableLanguage": "English"
    },
    "address": {
      "@type": "PostalAddress",
      "addressRegion": "Northern Ireland",
      "addressCountry": "GB"
    },
    "parentOrganization": {
      "@type": "Organization",
      "name": "Elevateo Co"
    }
  }
  </script>
```

Rules:
- Do NOT add `telephone` or `email` — neither appears in the visible page UI (research confirmed no visible contact details).
- Do NOT add a street address — only `addressRegion` and `addressCountry` are used, matching what the page says ("headquartered in Northern Ireland").
- `contactPoint.url` uses the Calendly booking link — this is the only contact method on the page.
- All strings are plain Unicode — no HTML entities (`&ndash;`, `&rsquo;`, etc.) anywhere in the JSON.
- Insert BEFORE `</head>`, AFTER the existing favicon/font/CSS link tags (around line 51 currently).
  </action>
  <verify>
    <automated>grep -c "application/ld+json" "index.html"</automated>
    Expected output: 1 (exactly one JSON-LD script block after this task)

    Also run:
    <automated>grep -c "LocalBusiness" "index.html"</automated>
    Expected output: 1

    <automated>grep -c "ProfessionalService" "index.html"</automated>
    Expected output: 1

    <automated>grep -c "areaServed" "index.html"</automated>
    Expected output: 1

    <automated>grep -c "Northern Ireland" "index.html"</automated>
    Expected output: at least 2 (one in schema, one in existing page content)

    JSON validity check — paste the JSON-LD block content (without the script tags) into https://validator.schema.org/ and confirm zero errors reported.
  </verify>
  <done>
    - index.html contains exactly one `<script type="application/ld+json">` block after this task
    - The block contains `@type: ["LocalBusiness", "ProfessionalService"]`
    - `name`, `url`, `logo`, `description`, `areaServed`, `contactPoint`, `address`, `parentOrganization` are all present
    - No `telephone`, `email`, or street address present in the schema
    - schema.org validator reports zero errors when the block is pasted in
  </done>
</task>

</tasks>

<verification>
After task completion:
1. Open index.html and confirm the `<script type="application/ld+json">` block appears before `</head>`
2. Paste the JSON-LD block content (without the wrapping `<script>` tags) into https://validator.schema.org/ — confirm zero errors
3. Confirm `grep -c "application/ld+json" index.html` returns 1
</verification>

<success_criteria>
- index.html contains one valid Organization + LocalBusiness JSON-LD block in `<head>`
- Block validates with zero errors at schema.org validator
- No contact details in schema that are absent from visible page content
- SCHEMA-01 requirement is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-structured-data/08-01-SUMMARY.md` using the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>

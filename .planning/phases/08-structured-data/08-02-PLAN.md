---
phase: 08-structured-data
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - index.html
autonomous: true
requirements:
  - SCHEMA-02
  - SCHEMA-03
must_haves:
  truths:
    - "index.html <head> contains a FAQPage JSON-LD block covering all 7 FAQ questions and answers"
    - "index.html <head> contains a Service JSON-LD block with a @graph array of 3 service entries"
    - "All FAQ answer text matches the visible page content with HTML entities decoded to Unicode"
    - "All three JSON-LD blocks (from plan 01 and 02) are syntactically valid JSON"
  artifacts:
    - path: "index.html"
      provides: "FAQPage + Service JSON-LD blocks in <head>"
      contains: "FAQPage"
  key_links:
    - from: "index.html <head>"
      to: "schema.org/FAQPage"
      via: "@type: \"FAQPage\", mainEntity array of 7 Question nodes"
      pattern: "FAQPage"
    - from: "index.html <head>"
      to: "schema.org/Service"
      via: "@graph array with 3 Service nodes"
      pattern: "\"@type\": \"Service\""
---

<objective>
Add FAQPage and Service JSON-LD blocks to index.html <head>, completing the three-block structured data set for Phase 8.

Purpose: FAQPage block signals Q&A content to crawlers (SCHEMA-02). Service block signals Leadzly's three service offerings to Google's entity understanding (SCHEMA-03). Both blocks must validate with zero errors.
Output: Two additional `<script type="application/ld+json">` blocks in index.html <head>, following the Organization+LocalBusiness block added in plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-structured-data/08-RESEARCH.md
@.planning/phases/08-structured-data/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert FAQPage JSON-LD block (7 Q&A pairs) into index.html head</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 1006–1078 — the FAQ section with all 7 question/answer pairs)
    - index.html (current end of head — find existing JSON-LD blocks from plan 01 to know insertion point)
    - .planning/phases/08-structured-data/08-RESEARCH.md (Pattern 2: FAQPage, HTML entity decoding table in Pitfall 1)
  </read_first>
  <action>
Insert the following JSON-LD block into index.html immediately before `</head>`, AFTER the Organization+LocalBusiness block added in plan 01.

The block must use `<script type="application/ld+json">`. Every answer `text` value is the decoded Unicode version of the HTML source — HTML entities must NOT appear in the JSON.

Entity decoding reference (apply consistently):
- `&ndash;` → `–` (en-dash)
- `&mdash;` → `—` (em-dash)
- `&rsquo;` → `'` (right single quote / apostrophe)
- `&ldquo;` → `"` (left double quote)
- `&rdquo;` → `"` (right double quote)

Exact block to insert:

```html
  <!-- Structured Data: FAQPage (SCHEMA-02) -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "FAQPage",
    "mainEntity": [
      {
        "@type": "Question",
        "name": "How quickly can you start generating meetings?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "We typically onboard new clients within 30 days. Week one is ICP audit and script development; week two is outreach infrastructure setup; week three is live calling. Most clients see their first qualified meetings in weeks 3–4."
        }
      },
      {
        "@type": "Question",
        "name": "What does \"qualified meeting\" actually mean?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "A qualified meeting is a booked appointment with a decision-maker who fits your ICP, understands what you do, and has agreed to the call. We don't count no-shows or rescheduled meetings that never happen."
        }
      },
      {
        "@type": "Question",
        "name": "Do I have to sign a long-term contract?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "No. All plans are month-to-month. We don't believe in locking clients in — if we're not delivering, you should be free to leave. Our 30-day performance guarantee reflects that confidence."
        }
      },
      {
        "@type": "Question",
        "name": "What industries do you work with?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "We've run campaigns across SaaS, recruitment, e-commerce, financial services, professional services, construction, managed IT, and more. If your product or service is sold B2B, we can build a campaign for it."
        }
      },
      {
        "@type": "Question",
        "name": "How is Leadzly different from a typical cold-calling agency?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Most agencies operate offshore and optimise for call volume. We're a UK-based team that optimises for meeting quality — every lead is qualified before it hits your calendar. You pay for results, not activity."
        }
      },
      {
        "@type": "Question",
        "name": "What information do you need to get started?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "We need your ICP (ideal customer profile), your core value proposition, and access to your calendar or CRM. We handle the rest — list building, scripting, outreach, and qualification."
        }
      },
      {
        "@type": "Question",
        "name": "What happens if you don't hit the meeting targets?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Our 30-day guarantee means if we don't deliver qualified meetings in your first month, you don't pay for that month. We stand behind our results."
        }
      }
    ]
  }
  </script>
```

Rules:
- The `name` field on each Question must match the visible question text on the page exactly (after entity decoding).
- The `text` field in each Answer must match the visible answer paragraph text exactly (after entity decoding).
- No HTML tags inside any JSON string value.
- No trailing comma after the last array item.
- Insert after the Organization+LocalBusiness block, before `</head>`.
  </action>
  <verify>
    <automated>grep -c "FAQPage" "index.html"</automated>
    Expected output: 1

    <automated>grep -c "\"@type\": \"Question\"" "index.html"</automated>
    Expected output: 7

    <automated>grep -c "acceptedAnswer" "index.html"</automated>
    Expected output: 7

    <automated>grep -c "application/ld+json" "index.html"</automated>
    Expected output: 2 (Organization+LocalBusiness from plan 01, plus this FAQPage block)

    JSON validity check — paste the FAQPage JSON block content into https://validator.schema.org/ and confirm zero errors.
  </verify>
  <done>
    - index.html contains a FAQPage JSON-LD block with exactly 7 Question entries
    - Each Question has a `name` (question text) and `acceptedAnswer.text` (answer text)
    - No HTML entities appear in any JSON string value
    - schema.org validator reports zero errors for the FAQPage block
    - Total JSON-LD script blocks in index.html is now 2
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert Service JSON-LD block (3 services) into index.html head</name>
  <files>index.html</files>
  <read_first>
    - index.html (lines 309–339 — services section with all three service card titles and descriptions)
    - index.html (current end of head — find existing JSON-LD blocks to know insertion point)
    - .planning/phases/08-structured-data/08-RESEARCH.md (Pattern 3: Service, Anti-Patterns section)
  </read_first>
  <action>
Insert the following JSON-LD block into index.html immediately before `</head>`, AFTER the FAQPage block added in Task 1.

The block uses a `@graph` array containing three Service nodes. Service names and descriptions are sourced verbatim from the services section of index.html (lines 313–329). Note: the page has a 4th service (Sales Enablement) — include it as a fourth Service node since it appears on the page. Do NOT invent services that are not on the page.

Exact block to insert:

```html
  <!-- Structured Data: Service offerings (SCHEMA-03) -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": "Service",
        "name": "Telesales Outsourcing",
        "description": "Professional cold callers who know your ICP inside out. We handle outbound calls, objection handling, and warm handoffs — so your calendar fills itself.",
        "provider": { "@type": "LocalBusiness", "name": "Leadzly" },
        "serviceType": "B2B Telesales",
        "areaServed": "United Kingdom"
      },
      {
        "@type": "Service",
        "name": "Meeting Booking",
        "description": "20+ qualified appointments per month with decision-makers in your target market. No tire-kickers, no wasted time.",
        "provider": { "@type": "LocalBusiness", "name": "Leadzly" },
        "serviceType": "B2B Appointment Setting",
        "areaServed": "United Kingdom"
      },
      {
        "@type": "Service",
        "name": "BDM Provision",
        "description": "Dedicated Business Development Managers embedded in your sales process. Full-time focus, fractional cost.",
        "provider": { "@type": "LocalBusiness", "name": "Leadzly" },
        "serviceType": "Business Development Management",
        "areaServed": "United Kingdom"
      },
      {
        "@type": "Service",
        "name": "Sales Enablement",
        "description": "Custom scripts, CRM setup, sales training, and playbooks. We build the systems that make your sales predictable.",
        "provider": { "@type": "LocalBusiness", "name": "Leadzly" },
        "serviceType": "Sales Enablement",
        "areaServed": "United Kingdom"
      }
    ]
  }
  </script>
```

Rules:
- Service `description` values must match the visible `.service-card__desc` text on the page exactly (em-dashes `—` are correct Unicode, not HTML entities).
- `provider.name` must be `"Leadzly"` (not the parent org, not "Elevateo Co").
- Do NOT add services that are not visible on the page.
- No trailing comma after the last array item.
- Insert after the FAQPage block, before `</head>`.
  </action>
  <verify>
    <automated>grep -c "\"@type\": \"Service\"" "index.html"</automated>
    Expected output: 4 (one per service node — Telesales Outsourcing, Meeting Booking, BDM Provision, Sales Enablement)

    <automated>grep -c "@graph" "index.html"</automated>
    Expected output: 1

    <automated>grep -c "application/ld+json" "index.html"</automated>
    Expected output: 3 (all three blocks: Organization+LocalBusiness, FAQPage, Service)

    <automated>grep -c "Telesales Outsourcing" "index.html"</automated>
    Expected output: at least 2 (one in visible page HTML, one in schema)

    JSON validity check — paste the Service JSON block content into https://validator.schema.org/ and confirm zero errors.
  </verify>
  <done>
    - index.html contains a Service JSON-LD block with a @graph array of 4 Service nodes (matching all 4 visible services on the page)
    - Each Service node has `name`, `description`, `provider`, `serviceType`, `areaServed`
    - All description values match the visible page text exactly
    - schema.org validator reports zero errors for the Service block
    - Total JSON-LD script blocks in index.html is now 3
    - All three requirements SCHEMA-01, SCHEMA-02, SCHEMA-03 are satisfied
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run `grep -c "application/ld+json" index.html` — must return 3
2. Run `grep -c "\"@type\": \"Question\"" index.html` — must return 7
3. Run `grep -c "\"@type\": \"Service\"" index.html` — must return 4
4. Paste each of the three JSON-LD blocks (one at a time, without script tags) into https://validator.schema.org/ — all three must report zero errors
5. Confirm no HTML entity strings (`&ndash;`, `&rsquo;`, `&mdash;`, `&ldquo;`, `&rdquo;`) appear inside any JSON-LD block: `grep -n "&[a-z]*;" index.html` — check that any matches are in the visible HTML body, not inside `<script type="application/ld+json">` blocks
</verification>

<success_criteria>
- index.html <head> contains all three JSON-LD blocks: Organization+LocalBusiness, FAQPage (7 Q&A), Service (4 services)
- All three blocks validate with zero errors at https://validator.schema.org/
- No HTML entities inside any JSON-LD script block
- SCHEMA-01, SCHEMA-02, SCHEMA-03 all satisfied
- Phase 8 complete — ready for Google Rich Results Test after deployment
</success_criteria>

<output>
After completion, create `.planning/phases/08-structured-data/08-02-SUMMARY.md` using the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>

---
phase: 09-performance-core-web-vitals
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - page.css
  - index.html
autonomous: false
requirements:
  - PERF-04
  - PERF-05

must_haves:
  truths:
    - "No floating CTA element exists anywhere in index.html or page.css"
    - "page.css is served as a minified single-line or minimal-whitespace file"
    - "index.html references page.css with version query string ?v=14"
  artifacts:
    - path: "page.css"
      provides: "Minified CSS with all Phase 9 Plan 01 changes preserved"
      min_lines: 1
    - path: "index.html"
      provides: "Updated stylesheet link with incremented version string"
      contains: "page.css?v=14"
  key_links:
    - from: "index.html stylesheet link"
      to: "page.css"
      via: "href=\"./page.css?v=14\""
      pattern: "page\\.css\\?v=14"
---

<objective>
Verify PERF-04 (floating CTA absent — zero CLS contribution), then minify page.css using CleanCSS online tool and increment the stylesheet version string to bust caches.

Purpose: PERF-04 is satisfied by absence — a quick grep confirms no floating CTA code was reintroduced. PERF-05 reduces page.css payload by ~40–60% (removing comments, whitespace, blank lines from 1,693+ lines), improving load time. The version bump ensures browsers fetch the new minified file immediately rather than serving a stale cached copy.
Output: Verified PERF-04 status documented, minified page.css, updated version string in index.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-performance-core-web-vitals/09-RESEARCH.md
@.planning/phases/09-performance-core-web-vitals/09-01-SUMMARY.md
</context>

<interfaces>
<!-- Key facts from codebase. No exploration needed. -->

Current stylesheet link in index.html (line 51):
```html
<link rel="stylesheet" href="./page.css?v=13">
```
Target after version bump:
```html
<link rel="stylesheet" href="./page.css?v=14">
```

PERF-04 context (from 09-RESEARCH.md): The floating sticky CTA was removed in commit 909aaf2 before Phase 7. It has never been re-added. The expected grep result is zero matches.

CleanCSS usage pattern (from 09-RESEARCH.md):
- URL: https://cleancss.com
- Optimisation level: DEFAULT (not "advanced") — preserves var(--...) custom property references
- Process: paste entire page.css → copy/download output → overwrite page.css in place
- Do NOT rename to page.min.css — index.html links to ./page.css
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify PERF-04 — confirm floating CTA is absent</name>
  <files>index.html, page.css</files>
  <read_first>
    - index.html (scan for any floating, fixed-position CTA patterns)
    - page.css (scan for floating-cta, fab-cta, or similar class patterns)
  </read_first>
  <action>
    Run the following grep commands to confirm no floating CTA element or CSS rule exists. All checks should return zero matches.

    Check 1 — Search index.html for floating CTA class patterns:
    ```
    grep -n "floating\|float-cta\|fab-cta\|sticky-cta\|fixed-cta" index.html
    ```
    Expected result: no output (zero matches)

    Check 2 — Search page.css for floating CTA class patterns:
    ```
    grep -n "floating\|float-cta\|fab-cta\|sticky-cta\|fixed-cta" page.css
    ```
    Expected result: no output (zero matches)

    Check 3 — Search page.css for position:fixed (to confirm no CTA uses fixed positioning):
    ```
    grep -n "position.*fixed" page.css
    ```
    Expected result: no output, or only lines unrelated to a CTA button (nav using position:sticky is fine)

    If all checks return zero matches for CTA-related patterns, PERF-04 is confirmed satisfied by absence. No code change required. Document the grep results in the SUMMARY as "PERF-04 satisfied by absence — floating CTA removed in commit 909aaf2".

    If any match is unexpectedly found, review it — if it is a real floating CTA element, the CLS-safe fix is: ensure it uses position:fixed (not static/relative), and reveals via CSS opacity/transform transition only (not display:none toggling). This situation is not expected based on git history.
  </action>
  <verify>
    <automated>grep -c "floating\|float-cta\|fab-cta\|sticky-cta" "C:/Work File/Elevateo Work/Leadzly - Company/index.html"</automated>
    <automated>grep -c "floating\|float-cta\|fab-cta\|sticky-cta" "C:/Work File/Elevateo Work/Leadzly - Company/page.css"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "floating|float-cta|fab-cta|sticky-cta" index.html returns 0
    - grep -c "floating|float-cta|fab-cta|sticky-cta" page.css returns 0
    - PERF-04 status documented in SUMMARY as "satisfied by absence — floating CTA removed in commit 909aaf2"
  </acceptance_criteria>
  <done>PERF-04 confirmed: no floating CTA exists in index.html or page.css. Zero CLS contribution from this element. Requirement satisfied by prior removal.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Minify page.css using CleanCSS online tool</name>
  <files>page.css</files>
  <action>
    Human-only action: paste page.css into CleanCSS online minifier, copy output, overwrite page.css in place.
    Step 1: Open https://cleancss.com in browser.
    Step 2: Paste the ENTIRE contents of page.css into the input field.
    Step 3: Set optimisation level to DEFAULT (not "advanced") — this preserves var(--...) CSS custom property references.
    Step 4: Click Minify.
    Step 5: Copy the entire minified output.
    Step 6: Overwrite page.css in the project root with the minified content. Do NOT rename to page.min.css — index.html links to ./page.css.
  </action>
  <how-to-verify>
    After saving the minified page.css:
    - Open the saved page.css — it should be a single line or very few lines with no blank lines or CSS comments
    - Search for var(--color-primary) — it must still be present (confirms custom properties were not stripped)
    - File size should be meaningfully smaller (~40-60% reduction expected from 1,693 lines)

    Type "minified" to resume execution.
  </how-to-verify>
  <verify>
    <automated>grep "var(--color-primary" "C:/Work File/Elevateo Work/Leadzly - Company/page.css"</automated>
  </verify>
  <done>page.css is a minified file: no CSS comments, no blank lines, CSS custom property references intact. File saved to project root overwriting the unminified version.</done>
  <resume-signal>Type "minified" when page.css has been overwritten with the minified content</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Increment stylesheet version string in index.html</name>
  <files>index.html</files>
  <read_first>
    - index.html (line 51, current stylesheet link)
  </read_first>
  <action>
    In index.html, update the stylesheet link to increment the version query string from v=13 to v=14.

    Current state (line 51):
    ```html
    <link rel="stylesheet" href="./page.css?v=13">
    ```

    Replace with:
    ```html
    <link rel="stylesheet" href="./page.css?v=14">
    ```

    This is the cache-busting mechanism for the project (pattern established in Phases 1–8). The query string change forces browsers and CDNs that have cached the old page.css?v=13 to fetch the new minified version. The filename stays page.css — do not change it to page.min.css.
  </action>
  <verify>
    <automated>grep "page.css?v=14" "C:/Work File/Elevateo Work/Leadzly - Company/index.html"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains exactly: href="./page.css?v=14"
    - index.html does NOT contain: href="./page.css?v=13" (old version string removed)
    - The line is inside the head section (not duplicated elsewhere)
  </acceptance_criteria>
  <done>index.html stylesheet link references page.css?v=14. Browsers will bypass any cached v=13 copy and fetch the new minified CSS file.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. grep -c "floating|float-cta|fab-cta" index.html — returns 0 (PERF-04 confirmed)
2. grep -c "floating|float-cta|fab-cta" page.css — returns 0 (PERF-04 confirmed)
3. grep "page.css?v=14" index.html — returns match (PERF-05 version bump)
4. wc -l page.css — line count significantly reduced from 1,693 (minification applied)
5. grep "var(--color-primary" page.css — returns match (custom properties preserved)
</verification>

<success_criteria>
- PERF-04 verified: grep confirms zero floating CTA elements in index.html and page.css
- page.css is minified: comment-free, whitespace-free, CSS custom property references intact
- index.html stylesheet link reads ./page.css?v=14
- All 5 requirements (PERF-01 through PERF-05) satisfied across Plans 01 and 02
- Visual appearance of site unchanged after minification (same CSS rules, same values, just compressed)
</success_criteria>

<output>
After completion, create `.planning/phases/09-performance-core-web-vitals/09-02-SUMMARY.md`
</output>

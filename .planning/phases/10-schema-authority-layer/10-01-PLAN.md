---
phase: 10-schema-authority-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - NAP-03
  - EEAT-02
  - SCHEMA-04

must_haves:
  truths:
    - "Google's Rich Results Test confirms LocalBusiness schema includes streetAddress, postalCode, addressLocality, addressCountry, and telephone"
    - "A Person entity for Allan Chan is present in <head> with jobTitle, url, and sameAs (LinkedIn) fields"
    - "A HowTo block is present in <head> with 4 steps whose names and descriptions match the visible How It Works section"
    - "All three JSON-LD blocks pass schema.org validator with zero errors"
  artifacts:
    - path: "index.html"
      provides: "Updated SCHEMA-01 LocalBusiness block + new Person block + new HowTo block in <head>"
      contains: "streetAddress.*1 Hollycroft Avenue"
    - path: "index.html"
      provides: "Person JSON-LD"
      contains: "Allan Chan"
    - path: "index.html"
      provides: "HowTo JSON-LD"
      contains: "HowToStep"
  key_links:
    - from: "index.html SCHEMA-01 address object"
      to: "PostalAddress full fields"
      via: "inline edit of existing address block"
      pattern: "streetAddress.*1 Hollycroft Avenue"
    - from: "index.html <head>"
      to: "Person block"
      via: "append after SCHEMA-03 closing </script>"
      pattern: "@type.*Person"
    - from: "index.html <head>"
      to: "HowTo block"
      via: "append after Person closing </script>"
      pattern: "@type.*HowTo"
---

<objective>
Add three JSON-LD changes to index.html to establish machine-readable authority signals: update the existing LocalBusiness schema with full postal address and telephone (NAP-03), add a Person entity for founder Allan Chan (EEAT-02), and add a HowTo block mapping the visible process steps (SCHEMA-04).

Purpose: Machines — including Google's knowledge graph, AI search crawlers, and structured data parsers — can identify Leadzly's location, contact details, founder identity, and service process from structured data alone. This is the foundation for E-E-A-T signals and AI citability.

Output: index.html with three updated/new JSON-LD blocks in <head>, all passing schema.org validator with zero errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-schema-authority-layer/10-RESEARCH.md
@.planning/phases/10-schema-authority-layer/10-VALIDATION.md

<interfaces>
<!-- Existing SCHEMA-01 block in index.html (lines 121–150). This is the EDIT TARGET for Task 1. -->
<!-- Comment anchor: <!-- Structured Data: Organization + LocalBusiness (SCHEMA-01) --> -->

Current address object (lines 140–144):
```json
"address": {
  "@type": "PostalAddress",
  "addressRegion": "Northern Ireland",
  "addressCountry": "GB"
},
```
No telephone field currently present.

<!-- After Task 1, the address block must be: -->
```json
"address": {
  "@type": "PostalAddress",
  "streetAddress": "1 Hollycroft Avenue",
  "addressLocality": "Belfast",
  "addressRegion": "Northern Ireland",
  "postalCode": "BT5 5JE",
  "addressCountry": "GB"
},
"telephone": "+13074009814",
```

<!-- Insertion point for Task 2 new blocks: immediately before </head> (line 256 in current file) -->
<!-- Three existing <script type="application/ld+json"> blocks are in head: SCHEMA-01, SCHEMA-02, SCHEMA-03 -->
<!-- New blocks go after SCHEMA-03 closing </script>, before </head> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update LocalBusiness address and telephone (NAP-03)</name>
  <files>index.html</files>
  <read_first>
    - index.html — read lines 121–150 (SCHEMA-01 block) to see the exact current address object before editing. The comment anchor is `<!-- Structured Data: Organization + LocalBusiness (SCHEMA-01) -->`. Confirm the block contains `"@type": ["LocalBusiness", "ProfessionalService"]` before making any edit.
  </read_first>
  <action>
Edit the existing PostalAddress object in the SCHEMA-01 block (LocalBusiness). Replace the current address object:

```json
"address": {
  "@type": "PostalAddress",
  "addressRegion": "Northern Ireland",
  "addressCountry": "GB"
},
```

With the full PostalAddress including all five sub-fields, and add a `telephone` field at the same level as `address`:

```json
"address": {
  "@type": "PostalAddress",
  "streetAddress": "1 Hollycroft Avenue",
  "addressLocality": "Belfast",
  "addressRegion": "Northern Ireland",
  "postalCode": "BT5 5JE",
  "addressCountry": "GB"
},
"telephone": "+13074009814",
```

The `telephone` line must be inserted immediately after the closing `},` of the address object and before the `"parentOrganization"` field that follows it.

**Exact field values (from STATE.md v1.2 note):**
- streetAddress: `1 Hollycroft Avenue`
- addressLocality: `Belfast`
- addressRegion: `Northern Ireland` (keep existing, do not remove)
- postalCode: `BT5 5JE`
- addressCountry: `GB` (keep existing, do not remove)
- telephone: `+13074009814` (E.164 format — no spaces, no dashes)

**Critical rules:**
- Edit ONLY the block with the comment `<!-- Structured Data: Organization + LocalBusiness (SCHEMA-01) -->`. Do NOT touch SCHEMA-02 (FAQPage) or SCHEMA-03 (Service) blocks.
- Do not add a second LocalBusiness block — edit the existing one in place.
- No HTML entities in JSON string values (none expected here, but verify).
- Keep `"parentOrganization"` field intact after the telephone line.
  </action>
  <verify>
    <automated>grep -n "streetAddress\|addressLocality\|postalCode\|telephone" "index.html"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "streetAddress" index.html` returns `"streetAddress": "1 Hollycroft Avenue"`
    - `grep "addressLocality" index.html` returns `"addressLocality": "Belfast"`
    - `grep "postalCode" index.html` returns `"postalCode": "BT5 5JE"`
    - `grep "telephone" index.html` returns `"telephone": "+13074009814"`
    - `grep -c "application/ld+json" index.html` returns `3` (count unchanged — edit, not add)
    - `grep "addressRegion" index.html` returns `"addressRegion": "Northern Ireland"` (preserved)
    - `grep "addressCountry" index.html` returns `"addressCountry": "GB"` (preserved)
    - `grep "parentOrganization" index.html` returns a match (field not accidentally deleted)
  </acceptance_criteria>
  <done>SCHEMA-01 LocalBusiness block contains full PostalAddress (all 5 sub-fields) and telephone in E.164 format. Total JSON-LD block count remains 3.</done>
</task>

<task type="auto">
  <name>Task 2: Add Person and HowTo JSON-LD blocks (EEAT-02, SCHEMA-04)</name>
  <files>index.html</files>
  <read_first>
    - index.html — read lines 215–260 (end of SCHEMA-03 block through </head>) to confirm the insertion point. The new blocks go immediately after the SCHEMA-03 `</script>` tag and before `</head>`.
  </read_first>
  <action>
Append two new `<script type="application/ld+json">` blocks immediately after the SCHEMA-03 closing `</script>` tag and before `</head>`. Insert them in this order: Person block first, HowTo block second.

**Block 1 — Person entity (EEAT-02):**

```html
  <!-- Structured Data: Person — Founder (EEAT-02) -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Person",
    "name": "Allan Chan",
    "jobTitle": "Founder",
    "url": "https://leadzly.co",
    "sameAs": "https://www.linkedin.com/in/allan-chan-865422216/"
  }
  </script>
```

**Block 2 — HowTo process (SCHEMA-04):**

```html
  <!-- Structured Data: HowTo — How It Works process (SCHEMA-04) -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "HowTo",
    "name": "From Zero to Pipeline in 4 Steps",
    "description": "How Leadzly books qualified B2B meetings for clients through a four-step outreach process.",
    "step": [
      {
        "@type": "HowToStep",
        "position": "1",
        "name": "ICP Audit",
        "text": "We analyse your ideal customer profile, market positioning, and competitive landscape.",
        "url": "https://leadzly.co/#how-it-works"
      },
      {
        "@type": "HowToStep",
        "position": "2",
        "name": "Custom Script",
        "text": "Our team builds tailored call scripts, email sequences, and objection-handling frameworks.",
        "url": "https://leadzly.co/#how-it-works"
      },
      {
        "@type": "HowToStep",
        "position": "3",
        "name": "Daily Outreach",
        "text": "Dedicated reps make 100+ calls per day, targeting qualified decision-makers.",
        "url": "https://leadzly.co/#how-it-works"
      },
      {
        "@type": "HowToStep",
        "position": "4",
        "name": "Qualified Handoff",
        "text": "Warm, qualified meetings land directly on your calendar. You just close.",
        "url": "https://leadzly.co/#how-it-works"
      }
    ]
  }
  </script>
```

**Critical rules:**
- All step text values are verbatim from index.html How It Works section (lines 570–591 per RESEARCH.md). No HTML entities — safe to use as-is.
- Property name is `"step"` (singular), NOT `"steps"` (plural). Using `"steps"` with an array is a known schema.org anti-pattern (see RESEARCH.md Pitfall 2).
- Each HowToStep MUST include `"@type": "HowToStep"` — without it, parsers cannot identify the step objects.
- `position` values are strings (`"1"`, `"2"`, `"3"`, `"4"`), not integers.
- Note on step count: The ROADMAP success criterion says "3 steps" but the visible How It Works section has 4 steps. Per RESEARCH.md recommendation, all 4 visible steps are included to satisfy "names and descriptions match the visible section content".
- No HTML entities anywhere in any JSON string value.
  </action>
  <verify>
    <automated>grep -c "application/ld+json" "index.html"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "application/ld+json" index.html` returns `5` (was 3, now 3 original + 2 new = 5)
    - `grep "@type.*Person" index.html` returns a match
    - `grep "Allan Chan" index.html` returns a match
    - `grep "jobTitle" index.html` returns `"jobTitle": "Founder"`
    - `grep "sameAs" index.html` returns `"sameAs": "https://www.linkedin.com/in/allan-chan-865422216/"`
    - `grep "@type.*HowTo" index.html` returns a match (not inside HowToStep — HowTo top-level type)
    - `grep -c "HowToStep" index.html` returns `4` (four step objects)
    - `grep '"step"' index.html` returns a match (singular "step" property, not "steps")
    - `grep "ICP Audit\|Custom Script\|Daily Outreach\|Qualified Handoff" index.html` returns 4 matches
    - `grep "EEAT-02\|SCHEMA-04" index.html` returns 2 matches (HTML comment labels present)
    - Both new blocks appear before `</head>` in the file (grep -n "</head>" returns line number higher than grep -n "EEAT-02")
  </acceptance_criteria>
  <done>Person block and HowTo block present in index.html head, each in a separate labelled script tag. Total JSON-LD block count is 5. All four HowToStep names match the visible How It Works section verbatim.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the following checks against index.html:

```bash
# Block count: must be 5
grep -c "application/ld+json" index.html

# NAP-03: all address fields present
grep -n "streetAddress\|addressLocality\|postalCode\|telephone" index.html

# EEAT-02: Person block present
grep -n "Allan Chan\|jobTitle\|sameAs" index.html

# SCHEMA-04: HowTo + all 4 steps present
grep -c "HowToStep" index.html
grep -n "ICP Audit\|Custom Script\|Daily Outreach\|Qualified Handoff" index.html

# No HTML entities in JSON blocks (must return 0)
grep -c "&amp;\|&ndash;\|&rsquo;\|&mdash;" index.html
```

**Manual validation (run after commit):**
1. Copy each of the three updated/new JSON-LD blocks from index.html and paste into https://validator.schema.org/ — each must show zero errors.
2. Visit https://search.google.com/test/rich-results and test the deployed URL — LocalBusiness must show as valid with address fields.
</verification>

<success_criteria>
- `grep -c "application/ld+json" index.html` returns `5`
- `grep "streetAddress" index.html` returns `"streetAddress": "1 Hollycroft Avenue"`
- `grep "telephone" index.html` returns `"telephone": "+13074009814"`
- `grep "Allan Chan" index.html` returns a match inside a Person block
- `grep -c "HowToStep" index.html` returns `4`
- All three changed/new JSON-LD blocks pass schema.org validator with zero errors
- No regressions: `grep -c "application/ld+json" index.html` is exactly 5 (not fewer)
</success_criteria>

<output>
After completion, create `.planning/phases/10-schema-authority-layer/10-01-SUMMARY.md` using the summary template.
</output>

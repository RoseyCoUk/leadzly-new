---
phase: 11-visible-identity-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [NAP-01, NAP-02, EEAT-01]

must_haves:
  truths:
    - "Footer shows full postal address as readable text: 1 Hollycroft Avenue, Belfast, BT5 5JE, United Kingdom"
    - "Footer phone number is a working tel: link displaying +1 307 400 9814 (placeholder +44 000 number removed)"
    - "About section names Allan Chan as founder with a visible, clickable LinkedIn link"
  artifacts:
    - path: "index.html"
      provides: "Footer NAP block + About founder attribution"
      contains: "1 Hollycroft Avenue"
  key_links:
    - from: "footer .footer__col Connect"
      to: "tel:+13074009814"
      via: "anchor href"
      pattern: "tel:\\+13074009814"
    - from: "about__text"
      to: "https://www.linkedin.com/in/allan-chan-865422216/"
      via: "anchor href"
      pattern: "linkedin\\.com/in/allan-chan-865422216"
---

<objective>
Surface Leadzly's physical address, real phone number, and named founder as visible on-page text so visitors and crawlers encounter the same NAP data that the Phase 10 JSON-LD already declares.

Purpose: Closes the gap between schema (machine-readable) and visible page content (human-readable + E-E-A-T signal). Google and AI search engines reward consistency between structured data and body content.
Output: Updated index.html with footer NAP block and About founder byline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add address block and fix phone/LinkedIn in footer Connect column</name>
  <files>index.html</files>

  <read_first>
    - index.html lines 1103–1152 (full footer markup — see current state before editing)
  </read_first>

  <action>
In the footer's "Connect" column (the `<div class="footer__col">` with `<h4>Connect</h4>`), make three changes:

1. **Replace the placeholder phone entry** (the `<li>` containing `tel:+44000000000`) with the real number:
   ```html
   <li><a href="tel:+13074009814">+1 307 400 9814</a></li>
   ```

2. **Fix the LinkedIn href** (currently `href="#"`) to point to Allan Chan's profile:
   ```html
   <li><a href="https://www.linkedin.com/in/allan-chan-865422216/" target="_blank" rel="noopener noreferrer">LinkedIn</a></li>
   ```

3. **Add a postal address block** immediately after the closing `</ul>` tag of the Connect column and before the closing `</div>` of that column. Use an `<address>` element so browsers and crawlers recognise the semantic type:
   ```html
   <address class="footer__address">
     1 Hollycroft Avenue<br>
     Belfast, BT5 5JE<br>
     United Kingdom
   </address>
   ```

Do NOT add any new CSS — the address block will inherit footer body styles. Do NOT alter any other footer columns or the footer__bottom section.
  </action>

  <verify>
    <automated>grep -n "tel:+13074009814\|1 Hollycroft Avenue\|allan-chan-865422216" "index.html"</automated>
  </verify>

  <acceptance_criteria>
    - index.html contains exactly `tel:+13074009814` (no spaces, no dashes) as the href value
    - index.html contains the visible text `+1 307 400 9814` as the anchor's display text
    - index.html contains `1 Hollycroft Avenue` as visible text inside an `<address>` element
    - index.html contains `Belfast, BT5 5JE` inside the same `<address>` element
    - index.html contains `United Kingdom` inside the same `<address>` element
    - index.html contains `https://www.linkedin.com/in/allan-chan-865422216/` as the LinkedIn href in the footer
    - The string `tel:+44000000000` no longer appears anywhere in index.html
    - The string `+44 (0) 000 000 000` no longer appears anywhere in index.html
  </acceptance_criteria>

  <done>Footer Connect column shows real phone as tel: link, full postal address as semantic address block, and LinkedIn points to Allan Chan's profile.</done>
</task>

<task type="auto">
  <name>Task 2: Add Allan Chan founder byline to About section</name>
  <files>index.html</files>

  <read_first>
    - index.html lines 530–562 (full About section markup — see current state before editing)
  </read_first>

  <action>
In the About section (id="about"), add a founder byline immediately after the closing `</p>` tag of the `about__text` paragraph and before the opening `<div class="about__diff">` tag.

Insert this exact HTML:
```html
<p class="about__founder">Founded by <a href="https://www.linkedin.com/in/allan-chan-865422216/" target="_blank" rel="noopener noreferrer" class="about__founder-link">Allan Chan</a> — sales leader with a track record in NI B2B outbound.</p>
```

Do NOT change the `about__text` paragraph, the section-header, or any of the `about__diff` cards. Do NOT add any new CSS — the byline will inherit existing paragraph styles from page.css.
  </action>

  <verify>
    <automated>grep -n "Allan Chan\|about__founder\|allan-chan-865422216" "index.html"</automated>
  </verify>

  <acceptance_criteria>
    - index.html contains the text `Allan Chan` in the About section (between section id="about" and the about__diff div)
    - index.html contains `about__founder` as a class on a `<p>` element
    - index.html contains `https://www.linkedin.com/in/allan-chan-865422216/` as an href inside the About section (distinct from the footer occurrence)
    - The anchor wrapping "Allan Chan" has `target="_blank"` and `rel="noopener noreferrer"`
    - The `about__text` paragraph content is unchanged (still begins with "Leadzly is a division of Elevateo Co")
  </acceptance_criteria>

  <done>About section shows "Founded by Allan Chan" as a visible, clickable paragraph linking to the correct LinkedIn profile.</done>
</task>

</tasks>

<verification>
After both tasks complete, run these checks against index.html:

```bash
# NAP-01: Address present as readable text
grep "1 Hollycroft Avenue" index.html

# NAP-02: Real phone as tel: link, placeholder gone
grep "tel:+13074009814" index.html
grep "tel:+44000000000" index.html  # must return no matches

# EEAT-01: Founder named with LinkedIn in About section
grep -A5 'id="about"' index.html | grep "Allan Chan"
grep "allan-chan-865422216" index.html
```

All NAP-01, NAP-02, and EEAT-01 requirements satisfied by a single file edit — consistent with Phase 10 JSON-LD which already declares the same address, telephone, and Person entity in structured data.
</verification>

<success_criteria>
1. Footer displays "1 Hollycroft Avenue, Belfast, BT5 5JE, United Kingdom" as readable text inside an `<address>` element
2. Footer phone is `<a href="tel:+13074009814">+1 307 400 9814</a>` — placeholder `+44 (0) 000 000 000` removed
3. Footer LinkedIn href points to `https://www.linkedin.com/in/allan-chan-865422216/` (not `#`)
4. About section contains a visible "Founded by Allan Chan" paragraph linking to the same LinkedIn URL
5. No new CSS files created, no build tools used, no external dependencies added
6. page.css version query string unchanged (no CSS changes in this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/11-visible-identity-contact/11-01-SUMMARY.md`
</output>
